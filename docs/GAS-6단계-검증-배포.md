# GAS 6단계 검증·배포 인계

## 교사 운영 순서

1. 제공된 **경량화 교사 템플릿**을 `파일 → 사본 만들기`로 복사한다. 학생 기록과 API 키가 없는 템플릿이다. 수업별로 사본을 나눈다.
2. `확장 프로그램 → Apps Script`에서 `setupProject()`를 실행하고 시트를 새로고침한다.
3. `질문 챗봇 → 교사 자료 입력`에서 지문·학년·성취기준을 저장한다. 필요한 낱말 풀이를 확인한다. 보충 설명 생성·승인은 선택 사항이다.
4. 자기 API 키를 메뉴에서 설정하고 연결을 확인한다. 키를 셀에 적지 않는다.
5. Apps Script에서 웹 앱을 **새로 배포**한다. 실행 사용자는 교사 본인, 접근 범위는 수업에서 필요한 학생 범위로 설정한다. `CONFIG.STUDENT_WEB_APP_URL`에는 자기 배포의 `/exec` 주소만 입력한다.
6. `학생 챗봇 주소 확인`에서 열고 `99-999`로 미리보기한다. 학생은 `3-12`처럼 반-번호를 입력한다. 같은 자료·반-번호는 새로고침해도 대화를 복원하며 마친 대화는 잠긴다.
7. **자료를 바꿀 때(10단계).** 교사 자료 입력에서 지문이나 버전을 바꿔 저장하면 그 자료의 학생 대화는 `TURNS_ARCHIVE` 시트로 옮겨지고 학생은 새로 시작한다. 제목·시작 질문만 고치면 대화는 이어진다. 같은 지문으로 다른 날 새로 시작하려면 메뉴 `관리. 이 자료의 학생 대화 보관(새로 시작)`. 학생에게는 새 링크를 주거나 새로고침하게 한다.
8. **학생 화면(9단계).** 답 끝의 근거 줄, "확인한 교사 자료" 토글, "생각→근거→다듬기" 단계 표시는 없다. 말투는 교사 자료의 대상 학년 기준. 2국면 관리 질문은 한 턴 걸러 하나, 세션당 4개. 글에 없는 질문은 글의 주제와 상관있을 때만 "글에는 안 나오지만" 하고 짧게 답하고, 상관없으면 글로 돌아오게 한다(기본 켜짐 · 끄려면 `CONFIG`에 `ALLOW_GENERAL_ANSWER` 행을 `FALSE`로).
9. 수업 중에는 운영 대시보드와 미해결 질문을 확인한다. `99-`로 시작하는 미리보기는 학생 집계에서 제외된다. 학생 질문과 응답은 `TURNS`에 함께 저장된다.

데이터 시트는 CONTRACT 표 기준 **9장**, CONFIG 키는 **13개**, TURNS 열은 **18개**다. 문서 본문의 옛 요약 숫자보다 계약의 실제 열 이름을 따른다. 응답은 전송 후 같은 화면에 표시되며 토큰 단위 스트리밍 방식은 아니다.

## 코드 보호와 공유

시트에 묶인 스크립트는 사본을 만들면 함께 복제되고 편집자는 고칠 수 있다. 템플릿을 사본으로 나눠 주는 연수 방식에서는 코드를 숨길 수 없다(숨기려면 코드를 소유자 계정의 웹앱 하나에 두고 교사 시트는 자료만 담는 구조로 바꿔야 한다). 그래서 다음으로 정리한다.

- 코드는 공개하고 **비밀은 API 키에만** 둔다. 키는 Script Properties에만 있고 코드·시트·문서에 없다.
- 이용 조건은 `NOTICE.md`와 `Code.js` 머리말에 있다: 연수 참가자 본인 수업 목적에 한해 사본 사용·수정 허용, 제3자 재배포·공개 게시·상업적 이용·다른 서비스 이식 금지, 출처·머리말 유지. 저장소는 공개지만 라이선스를 두지 않아 기본이 "모든 권리 보유"다.
- 템플릿 시트를 사본 없이 보기만 시키려면 공유 설정에서 **뷰어 + "다운로드·인쇄·복사 옵션 표시" 끄기**. 단, 그러면 교사가 자기 사본을 만들 수 없다.
- 학생은 웹앱 주소만 쓰므로 코드에 닿지 않는다.

교사 템플릿 스크립트(독립 스크립트, 빈 시트)는 2026-09-06 배포 버전 30 코드로 갱신했다(`gas/` 21개 파일을 그대로 push).

## 재현 가능한 실제 AI 점검

Apps Script 편집기에서 다음 순서로 실행한다. 모두 교사 컨텍스트를 요구한다.

1. `prepareStageSixCopy()` — 운영 시트 사본에 급식 점검 지문을 넣는다. 운영 자료는 바꾸지 않는다.
2. `runStageSixCasesA()` → `runStageSixCasesB()` → `runStageSixCasesC()` — 같은 미리보기 세션에서 15개 발화를 순서대로 실행한다. 응답과 TURNS의 일치, 개인정보 가림, 국면·근거·AI 상태를 로그로 확인한다.
3. 재시도는 새 사본부터 시작한다. 중간 실패 뒤 이미 저장된 발화를 다시 보내지 않는다.
4. 학생 화면의 반-번호 입력·새로고침 복원·좁은 화면 배치·브라우저 오류를 별도로 확인한다. 원래 standalone 스크립트의 18개 항목은 발화 15개와 화면 점검 3개이며, GAS에 없는 교사 화면 복귀 버튼은 동일하게 검사할 수 없다.
5. 30개 학생 탭은 `99-601`~`99-630`을 사용한다. 모두 전송 준비 후 동시에 보내고 `runStageSixLoadReport()`로 30쌍의 기록·AI 성공·미리보기 제외를 확인한다. 실제 30개 기기/학교망 성능과는 구별한다.

검증용 사본 주소는 Apps Script 실행 로그에만 남긴다. 학생 기록·교사 시트 주소·API 키는 PR에 싣지 않는다.

## 6단계에서 발견해 수정한 오류

- 띄어쓰기 있는 승인 낱말(예: 선택 배식)을 찾지 못하던 검색 정규화.
- 앞말을 이어 묻는 뜻·이유 질문의 검색 맥락 누락. 직전 질문은 검색에만 보태고 학생 발화는 원문을 기록한다.
- 제목에 나온 퍼센트 질문이 잡담으로 분류되던 경우. 현재 자료의 수치 질문을 사실 질문으로 처리한다.
- 제목 수치는 승인된 MATERIALS 행의 materialId를 근거 ID로 쓰고 위치는 코드가 `자료 제목`으로 붙인다. 새 구간 생성 시 `10.4` 같은 소수점을 보존한다.
- `몰라요`의 도움 요청 분류, 대필과 개인정보의 서로 다른 안내.
- 모델이 관리 질문을 복사해 잘린 문장이 남지 않도록 답변 부분만 생성하게 하는 지시.
- 반-번호 화면에 남아 있던 옛 별명·카드 안내.

보호 파일 `Phase.js`, `Signals.js`, `Maintenance.js`, `evals/gas/`는 변경하지 않았다. 지시어·숫자 질문의 **국면 집계**는 Signals 규칙을 그대로 따르므로 검색 판정과 차이가 날 수 있다. 예: 세 번째 발화 “이 글에서는 어떤 뜻이야”에 답한 뒤 B1 질문이 붙는다. 이 집계 규칙 변경은 Claude 소유 파일의 별도 검토 대상이다.

## 배포 기록

2026-09-05, 기존 공개 주소를 **버전 18**로 갱신했다. 운영 시트는 전체 사본을 만든 후 경량화 열로 전환했다. 이전 미사용 시트는 보관하며 새 코드의 읽기·쓰기 대상이 아니다.

| 검사 | 결과 |
| --- | --- |
| 로컬 GAS 회귀 | PASS 31 · FAIL 0 · SKIP 0 |
| 웹앱/GAS 동일성 | 49회기 207턴, 국면·종류·문구·집계 불일치 0 |
| Claude 추가 공개 진입점 검사 | `npm run eval:gas:e2e` PASS 21 · FAIL 0 |
| 런타임 통합 검사 | 세션·AI·근거·저장·검토·이관·6단계 오류 재현 통과 |
| 실제 Apps Script 스모크 | 32 PASS |
| 실제 AI 순차 발화 | 15개 응답·TURNS 일치, 미리보기 표시 확인. 발견 오류 수정 후 재실행. 숫자 항목은 추가 실행에서 실제 AI 정상 답변 확인 |
| 공개 웹앱 30탭 | 30/30 답변, 30쌍 저장, AI 성공 30, 누락 0 |
| 30탭 응답 시간 | 중앙값 약 22초, p95 41.313초, 최대 42.235초 |
| 학생 화면 | 반-번호 필수, 새로고침 대화 복원, 390px 가로 넘침 없음, 지문/대화 전환 통과 |
| 브라우저 오류 | 0 |
| 미리보기 집계 | 30명 모두 학생 대시보드에서 제외 |
| 교사 템플릿 | 새 빈 시트와 독립 스크립트에 21개 파일 연결 완료. 학생 기록·키·원래 교사 주소 없음 |

30탭은 별도 Chrome 프로필의 실제 웹앱 화면에서 동시에 보내기를 눌렀다. 클릭 작업 호출 간격은 2ms이며, 서버 내부 동시 실행 수를 별도 측정한 값은 아니다. 30개 기기·학교 네트워크 검사는 하지 않았다. 토큰 스트리밍은 없으며 답변 완성 후 표시한다.

브라우저 재현: Playwright를 별도 폴더에 설치하고 `PLAYWRIGHT_MODULE`, `GAS_WEB_APP_URL`, `GAS_REPORT_DIR`를 지정해 `node scripts/check-gas-browser.cjs` 실행. 이미 사용한 99-601~630으로 부하 검사를 재실행하지 않는다. 화면만 확인하려면 `GAS_UI_ONLY=1`, `GAS_TAB_COUNT=1`. JSON·모바일 스크린샷은 지정한 보고서 폴더에 저장된다.

공개 배포 후 되돌려야 한다면 코드 버전만 17로 내리지 않는다. 17은 옛 시트 열을 쓰므로 백업 시트와 코드 버전을 함께 복구해야 한다.

Claude의 최신 검토(`7692334`)도 병합·검증했다. 차단하지 않는 후속 개선으로 어형이 바뀐 질문의 관련성 판정과 AI를 끈 B1/B2의 바탕 문장 다듬기가 남는다. 규칙 경로에서 같은 질문에 같은 답을 주는 동작은 유지한다.
