# 교사용 경량 질문챗봇 배포앱 PRD

## 1. 한 줄 정의

교사가 코드를 수정하지 않고 자신의 API, 수업 목표, 성취기준, 평가기준, 평가 근거와 수업자료를 입력한 뒤 학생용 질문챗봇을 배포하고, 학생 응답과 평가 결과는 교사 소유 Google Sheet에서 관리하는 연수용 경량앱이다.

### 현재 구현 현황

| 단계 | 상태 | 남은 일 |
| --- | --- | --- |
| 0. PRD·별도 구조 | 완료 | — |
| 1. 교사 설정 마법사 | 완료 | 실제 Google Sheet 사본에서 사용성 검수 |
| 2. 학생 세션·개인 Sheet 기록 | 코드 완료 | 실제 Apps Script 동시 기록 검수 |
| 3. 중앙 정책 엔진 | 코드 완료 | 운영 서버 배포 후 주소를 배포본에 고정 |
| 4. 개인 API 모델 호출 | 코드 완료 | 교사 테스트 API로 실제 응답·사용량 검수 |
| 5. 교사 평가 검수 | 기본 기능 완료 | 종합 세특·학기 누적 성장 보드는 후속 |
| 6. 배포 안내 | 일부 완료 | QR, 수업 복제, 배포 종료 기능 |
| 7. 연수 실증 | 미착수 | 초보 교사 3명·학생 역할 30명 실증 |

현재 구현은 기존 v37 배포본과 분리되어 있으며, 운영 중앙 엔진 주소가 확정되기 전에는 연수생에게 최종 배포하지 않는다.

## 2. 배경과 해결할 문제

현재 공개된 질문챗봇 v37은 연수 1차시의 공통 체험에는 적합하지만, 교사마다 다른 수업과 평가를 직접 구성하는 배포 도구는 아니다. 기존 전체 Apps Script를 복사하게 하면 코드가 크고 설정 과정이 복잡하며, 챗봇 정책을 고칠 때 모든 교사 사본을 다시 수정해야 한다.

경량앱은 다음 세 가지를 분리한다.

1. **교사 소유 영역**: 개인 API 키, 수업·평가 설정, 학생 응답, 교사 최종 판단
2. **중앙 관리 영역**: 대화 국면, 질문 정책, 안전 규칙, 프롬프트 버전
3. **학생 공개 영역**: 반-번호 입력과 질문·답변 화면

## 3. 제품 목표

- 교사가 2차시 안에 자신의 수업을 설정하고 미리보기까지 마친다.
- 교사가 코드 편집기나 시트 열 구조를 몰라도 사용할 수 있다.
- 백워드 설계 순서인 `목표 → 성취기준 → 평가기준 → 평가 근거 → 수업자료 → 운영 모드`를 화면이 안내한다.
- API 키는 교사 소유 Apps Script의 Script Properties에만 저장한다.
- 학생 대화와 평가 결과는 교사 소유 Google Sheet에만 저장한다.
- 중앙 정책을 갱신해도 교사 사본을 다시 배포하지 않도록 한다.
- 자동 판단은 교사의 최종 평가가 아니라 검토 초안으로 표시한다.

## 4. 비목표

- 학교 계정의 OAuth·관리자 승인을 자동으로 우회하지 않는다.
- 학생 이름, 이메일, 전화번호를 수집하지 않는다.
- 중앙 서버에 교사의 API 키나 학생 대화 원문을 장기 저장하지 않는다.
- 1차 버전에서 PDF·Word·이미지 평가까지 한꺼번에 구현하지 않는다.
- 1차 버전에서 여러 수업을 동시에 운영하지 않는다. 한 사본은 한 활성 수업을 기본으로 한다.

## 5. 연수 흐름

| 차시 | 교사 경험 | 사용 제품 | 완료 결과 |
| --- | --- | --- | --- |
| 1차시 | 연수자가 학생으로 참여해 평가모드·탐색모드를 비교한다 | 강사 중앙 챗봇 v37 | 질문과 응답이 평가 근거가 되는 과정 이해 |
| 2차시 | 개인 API 연결 후 백워드 설계와 수업자료를 입력하고 `99-999`로 점검한다 | 교사용 경량앱 | 개인 수업 챗봇 설정 완료 |
| 3차시 | 학생용 링크를 배포하고 결과 시트에서 자동 판단을 검수·수정한다 | 경량앱 + 교사 Sheet | 자동 판단 → 교사 검수 → 최종 평가 경험 |
| 후속과제 | 새 수업을 복제해 3명 이상에게 적용하고 다음 수업 개선안을 쓴다 | 교사 소유 사본 | 반복 가능한 개인 운영 완성 |

## 6. 사용자 화면

### 6.1 교사 설정 앱 — Google Sheet 전용 비공개 화면

학생에게 공개되는 웹주소에 관리자 화면을 넣지 않는다. 교사는 자신이 소유한 Google Sheet의 `경량 질문챗봇` 메뉴에서 설정 앱을 연다.

1. **API 연결**: 키 저장, 연결 확인, 키 삭제
2. **평가 설계**: 수업 목표, 성취기준, 평가기준, 수준별 기준, 수집할 평가 근거
3. **수업자료**: 제목, 본문, 자료 링크, 시작 질문, 평가/탐색 모드
4. **배포 전 점검**: 누락 항목, 중앙 엔진, 학생용 배포주소, 미리보기 기록 확인

### 6.2 학생 웹앱 — 공개 화면

- 반-번호만 입력한다. 예: `3-12`
- 교사가 승인한 수업자료를 읽고 질문한다.
- 학생 화면에는 API 키, 교사 평가기준 원문, 교사용 메모를 보내지 않는다.
- `99-`로 시작하는 코드는 미리보기이며 실제 통계에서 제외한다.

### 6.3 교사 결과 Sheet

| 시트 | 목적 |
| --- | --- |
| `시작하기` | 설정 순서와 현재 준비 상태 |
| `수업 자료` | 현재 수업의 백워드 설계와 자료 원본 |
| `학생별 현황` | 참여·질문 수·마지막 활동 |
| `질문과 답변` | 학생과 챗봇의 전체 대화 근거 |
| `교사 평가` | 자동 판단, 근거, 교사 판단, 피드백, 향상 방법, 다음 수업 |

API 키는 어느 시트에도 기록하지 않는다.

## 7. 기능 요구사항

| ID | 요구사항 | 완료 조건 |
| --- | --- | --- |
| FR-01 | 사본 최초 실행 시 필요한 다섯 시트를 만든다 | 같은 메뉴를 다시 실행해도 중복 시트·머리글이 생기지 않음 |
| FR-02 | 개인 API 키를 안전하게 저장·시험·삭제한다 | 키가 HTML 응답·시트·로그에 노출되지 않음 |
| FR-03 | 백워드 설계 필수값을 입력한다 | 목표·성취기준·평가기준·수준별 기준·평가 근거가 없으면 저장 차단 |
| FR-04 | 수업자료와 운영 모드를 입력한다 | 30자 이상 자료와 시작 질문, 평가/탐색 모드 저장 |
| FR-05 | 교사 설정 화면에서 준비 상태를 한눈에 보여 준다 | 설정 준비/배포 준비를 구분하고 누락 항목을 한국어로 안내 |
| FR-06 | 학생 화면에는 필요한 수업정보만 제공한다 | API 키·루브릭·교사 메모 제외 |
| FR-07 | 중앙 엔진에서 대화 계획을 받는다 | 엔진 실패 시 AI 호출 없이 재시도 안내와 실패 상태 저장 |
| FR-08 | 교사 Apps Script가 개인 API로 모델을 호출한다 | API 키가 중앙 엔진으로 전달되지 않음 |
| FR-09 | 대화와 자동 판단을 교사 Sheet에 기록한다 | 학생 1턴당 학생·챗봇 2행, 미리보기 구분 |
| FR-10 | 교사가 자동 판단을 수정하고 최종 확정한다 | 자동 판단과 교사 판단을 별도 열에 보존 |
| FR-11 | 학생 링크·QR과 배포 점검표를 제공한다 | 필수 준비가 끝나기 전에는 배포 완료로 표시하지 않음 |

## 8. 중앙 엔진 경계

교사 API 키를 중앙 서비스에 보내지 않기 위해 호출을 둘로 나눈다.

1. 경량앱이 익명화한 대화 상태와 수업 설정을 중앙 엔진의 `/v1/plan`에 보낸다.
2. 중앙 엔진은 정책 버전, 다음 대화 국면, 안전 판정, 모델에 보낼 지시문을 돌려준다.
3. 경량앱이 교사 API 키를 사용해 모델을 직접 호출한다.
4. 경량앱이 응답과 평가 근거를 교사 Sheet에 저장한다.

중앙 엔진은 기본적으로 무상태로 동작하고 학생 대화 원문을 장기 보관하지 않는다. 중앙 엔진이 멈추면 경량앱은 임의의 답을 만들지 않고 “잠시 뒤 다시 시도해 주세요”라고 안내한다.

## 9. 개인정보·보안 원칙

- 반-번호 외 개인정보 입력을 안내하지 않고, 입력된 민감정보는 모델 호출 전에 가린다.
- 관리자 설정 화면은 공개 학생 웹앱 경로로 제공하지 않는다.
- API 키는 Script Properties에만 저장하고 읽기 API로 되돌려 주지 않는다.
- 학생용 웹앱은 교사 계정으로 실행해 학생에게 Sheet 권한 승인을 요구하지 않는다.
- 중앙 엔진 요청에는 API 키, 교사 이메일, Google Sheet ID를 포함하지 않는다.
- 교사 보드에는 학생 질문 전문을 공개하지 않고, 전문은 교사 Sheet에서만 확인한다.

## 10. 오류·비용 정책

- 평가모드는 승인된 수업자료 중심으로 답하고 자료 밖 모델 호출을 줄인다.
- 탐색모드는 수업 주제와 관련된 확장 질문에만 모델을 사용한다.
- 한 학생 턴에 모델 호출은 최대 한 번이다.
- 중앙 엔진, API, Sheet 기록 중 어디서 실패했는지 교사 결과에 구분한다.
- 같은 요청의 중복 제출을 요청 ID로 막는다.
- API 제한이나 동시 접속 오류는 학생에게 쉬운 재시도 문구로 표시한다.

## 11. 구현 순서와 완료 기준

### 0단계 — PRD와 별도 프로젝트 구조

- `gas-lite/`를 기존 v37의 `gas/`와 분리한다.
- 기존 공개 배포와 시트 계약을 변경하지 않는다.

### 1단계 — 교사용 설정 마법사

- 다섯 시트 자동 준비
- API 저장·연결 확인·삭제
- 평가 설계 및 수업자료 저장
- 설정 준비도 표시
- 학생 미리보기 껍데기

완료 기준: 잘못된 필수값이 저장되지 않고, 저장 후 다시 열었을 때 값이 유지되며, 학생용 데이터에 API 키와 평가기준이 포함되지 않는다.

### 2단계 — 학생 대화와 개인 Sheet 기록

- 학생 코드와 세션 생성
- 학생/챗봇 턴 저장
- `99-` 미리보기 제외
- 중복 제출 방지

### 3단계 — 중앙 정책 엔진

- `/v1/plan` 요청·응답 계약 구현
- 평가/탐색 모드와 2국면 정책 이식
- 장애 시 안전한 중단

### 4단계 — 개인 API 모델 호출

- 중앙 계획을 받아 교사 Apps Script가 모델을 한 번 호출
- 비용·오류 상태 기록
- 49회기 정책 회귀 평가

### 5단계 — 교사 검수와 평가 포워드

- 자동 판단·근거 초안
- 교사 수정·최종 확정
- 학생 향상 방법과 다음 수업 제안
- 1회성 세특 및 누적 성장 자료 내보내기

### 6단계 — 배포 안내

- 학생 링크·QR
- 사전 점검과 `99-999` 미리보기
- 수업 종료·복제

### 7단계 — 연수 실증

- 교사 초보자 3명 설정 관찰
- 학생 역할 30명 동시 접속
- 설정 완료 시간, 실패 지점, API 비용, 응답시간 기록

## 12. 출시 판정

다음 조건을 모두 만족해야 연수생 배포본으로 표시한다.

- 초보 교사가 설명을 보며 20분 안에 설정·미리보기를 완료
- API 키가 Sheet·학생 HTML·중앙 엔진 요청에서 발견되지 않음
- 30명 동시 접속에서 기록 누락 0건
- 평가모드·탐색모드 회귀 평가 통과
- 자동 판단을 교사가 검수하지 않은 상태가 “최종 평가”로 표시되지 않음
- 학생 결과와 향상 방법이 동일 학생의 누적 성장 기록으로 이어짐
