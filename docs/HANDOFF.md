# 인계 메모 · 2026-08-21

다른 기기에서 이어받을 때 **이 문서 하나만 읽으면** 됩니다.

---

## 지금 어디까지 왔나

연수 패키지를 두 갈래로 나눴습니다.

| | 내용 | 상태 |
| --- | --- | --- |
| **Track A** | Notion만으로 하는 연수 (40명 · 3시간 · 진행자 시연 중심) | **진행 중** |
| **Track B** | Supabase + Vercel 웹앱, 바이브 코딩 연수용 | **보류** — `docs/TRACK_B_WEBAPP.md` |

Track A 작업 기록은 `docs/TRACK_A_NOTION_SETUP.md`에 **무엇을 · 왜 · 교사에게 어떻게 설명할지**로
전부 남겨 두었습니다. 연수 자료와 PPT는 그 문서에서 뽑아 쓰면 됩니다.

---

## 필요한 주소와 ID

### Notion

| 대상 | ID |
| --- | --- |
| 🧭 오늘의 연수 (연수생용 페이지) | `3c2c5f01a4a481ab80fcd82f13957eb6` |
| 💬 학생 의견쓰기 · 생각 나누기 (DB, 구 "학생 댓글") | `7f88a558-0743-440a-a98b-ed759275b301` |
| └ 데이터소스 | `bbc36f7a-4ca3-4e3d-a2d6-2d8f68f72236` |
| 🧑‍🎓 학생 명단 · 참여 현황 (DB) | `f9f36374f8c249f39c796ea211fa4730` |
| └ 데이터소스 | `59cd24eb-59c8-4a5b-a593-fa19ffb28043` |
| 📰 뉴스 읽기 자료실 (DB) | `371009b2-ea64-4510-b15c-dc37e07f3737` |
| └ 데이터소스 | `783b615a-c13b-4ae7-b51f-51ae9eec0b28` |
| 📏 교사용 평가 루브릭 v2.0 | `3c2c5f01a4a481f59454ea73b94274db` |
| 📝 새 기사 템플릿 | `3c2c5f01a4a481839dabfeeeec3ad063` |
| 🧭 PRD v0.2 (두 활동 중심 개정) | `3c2c5f01a4a481b8b728e07eb284c9a4` |

학생 제출 폼 (게시됨):
`https://judicious-ursinia-2c0.notion.site/33c361cb8284417397a238f50f856fed`

### 웹앱

- 프로덕션: `https://newscomment-ai.vercel.app` (시연용, 동작 확인 완료)
- 저장소: `github.com/gomgomeet/newscomment-ai`
- Supabase 프로젝트 ref: `xxbizrmgqfjqwiqxgiwe`

---

## 지금 당장 사용자가 해야 할 일 (Notion UI 필요)

제가 도구로 할 수 없어 막힌 것들입니다.

### 1. `교사 총점` 중복 정리

`💬 학생 댓글` DB에 `교사 총점`이 **두 개** 있습니다.

| 속성 | 유형 | 처리 |
| --- | --- | --- |
| `교사 총점` | 숫자 | **삭제** (값 없음) |
| `교사 총점 1` | 수식 | 남기고 이름을 `교사 총점`으로 |

표 뷰에서 열 머리글 클릭 → 속성 삭제. 이름 변경과 타입 변경이 한 배치에서 엇갈려 생긴 중복입니다.

**이게 끝나야** 학생 명단에 `평균 총점` 롤업(`댓글` → `교사 총점` → 평균)을 붙일 수 있습니다.

### 2. 폼 질문 구성 + 테스트 제출

`✍️ 학생 댓글 제출` 폼에 질문 세 개가 들어가야 합니다.

- `학생` (관계 드롭다운) — **타이핑이 아니라 선택**이어야 집계가 맞습니다
- `연결된 기사` (관계 드롭다운)
- `댓글`

폼 질문 구성은 MCP 도구로 되지 않아 UI에서만 가능합니다.

구성 후 **반드시 테스트 제출 한 번** 해보세요. 폼 링크가 이미 기사 6건 하단에 걸려 있어서,
질문이 잘못되어 있으면 학생이 제출해도 미제출로 잡힙니다.

빈 폼 뷰 `양식 작성기`도 안 쓰시면 정리하세요.

---

## 다음에 할 일 (우선순위 순)

### 1. 연수생 페이지에 실제 링크 채우기

`🧭 오늘의 연수` 페이지에 아직 링크가 비어 있습니다.

- 단계 1 — 템플릿 복제 링크
- 단계 3 — 폼 링크 (이미 기사 하단에 있으나 페이지에도 두면 편함)
- 단계 4 — 루브릭 v2.0 링크

### 2. 연수 진행자용 진행안

`docs/TRACK_A_NOTION_SETUP.md`에 시간표 초안과 위험 요소가 정리되어 있습니다.

**연수 날짜가 정해져야** 사전 과제 발송 시점을 역산할 수 있습니다. 가장 큰 위험은
**당일 Notion 계정 생성**입니다. 40명이 동시에 가입하면 이메일 인증 지연으로 20~30분이
증발합니다. 실제로 이번 작업 중 테스트 계정을 만들다 `email rate limit exceeded`에 걸렸습니다.

### 3. 교사용 PPT

**후순위입니다.** 완성된 DB 화면을 캡처해야 하므로 위 항목들이 먼저 끝나야 합니다.
지금 만들면 나중에 다시 캡처해야 합니다.

### 4. Track B 재개 (별도 연수)

`docs/TRACK_B_WEBAPP.md` 참조. 가장 먼저 정할 것은 **fork냐 호스팅이냐**이고,
OAuth·기록 주체·Deploy 버튼이 전부 거기서 갈립니다.

---

## 주의할 것

**`main` 브랜치에 푸시하면 Vercel 프로덕션이 자동 배포됩니다.** 2026-08-21 기준 자동배포를
멈춰 둔 상태이므로, 의도적으로 배포할 때만 `main`에 푸시하세요. 평소 작업은 `milestone-1`에만
푸시합니다.

**마이그레이션 003(`rubrics.auto_generated`)은 Supabase에 적용되지 않았습니다.** 다른 세션이
만든 루브릭 자동 생성 기능이 이 컬럼을 쓰는데, 적용 전에는 동작하지 않습니다. 그 기능은
PRD에 없는 항목이라 Track B 재개 시 방향을 먼저 정리해야 합니다.

**테스트 잔여물**은 Track B 재개 때 쓸 수 있어 그대로 두었습니다.

- Supabase: 테스트 프로젝트 `efb1978b-26d5-4858-b651-eb8bf0492806`, 계정 `notion.import.test@outlook.com`
- Notion: 샘플 학생 `S901`~`S906`과 댓글 6건 — **이건 연수 채점 실습에 쓰므로 지우지 마세요**

**`곰곰이쌤` 행**은 휴지통에 있습니다. 원문 클립 하나가 이 행을 가리키던 관계가 끊긴 상태라
정리가 필요할 수 있습니다.

## 2026-08-21 교재 1차시 개정 반영

사용자 요청으로 Notion 교재 페이지 `NIE 연수 교재` 맨 위에 1차시 개정 섹션을 삽입했다.

대상:
- 교재: https://judicious-ursinia-2c0.notion.site/NIE-3c3c5f01a4a481feadb1d550dbedc406
- 수업 템플릿: https://app.notion.com/p/3c3c5f01a4a48081b6b6f85bef8bc863
- 사례 1: https://mighty-sled-cee.notion.site/26f503723d3280d4a93aeef5ded85b4e

반영한 흐름:
1. 도구보다 목적이 먼저라는 에듀테크/생성형 AI 활용 관점
2. 바뀐 지식샘터 연수의 핵심: 플랫폼 학습보다 바로 적용 가능한 수업 패키지
3. NIE를 읽기·생각 연결·표현 평가로 바라보는 관점
4. 신문기사 수업에서 살펴볼 성취기준: 수업 장면을 성취기준 언어와 댓글 속 증거로 연결
5. 신문기사 Notion 수업 패키지 목표와 전체 흐름
6. 사례 1: 민주주의 참여 기사 기반 생각 표현 활동
7. 사례 2: 신문기사 댓글과 수행평가를 연결하는 오늘의 템플릿
8. 연수 후 가져갈 자료 목록

주의:
- 기존 페이지 전체를 replace 하지 않고 `insert_content`로 맨 위에 추가했다. 기존 교재 내용은 보존되어 있다.
- 다음 단계에서는 기존 교재 아래쪽 내용과 새 1차시 섹션의 중복/충돌을 정리해야 한다.
- 수업 템플릿 페이지 자체는 아직 새 교재 흐름에 맞춰 검수하지 않았다.
- 성취기준 섹션은 성취기준 코드를 나열하지 않고, 1차시 도입용으로 `수업 장면 → 관련 배움 → 댓글 속 증거`
  구조로 넣었다. 실제 코드 목록은 별도 성취기준 모음 페이지와 연결하면 된다.

## 2026-08-21 교재 2차시·3차시 구성 반영

사용자 요청으로 `NIE 댓글 형성평가 — 연수 운영 페이지` 맨 위에 전체 3차시 운영안과 2·3차시 상세 섹션을 삽입했다.

2차시: 수업의 실제 적용 — Notion 템플릿 세팅하기
- 학생 Notion 가입과 계정 이름 변경
- Notion 페이지에 신문기사 가져오기: 웹 클리퍼 / 생성형 AI 기사 탐색
- 평가 루브릭을 Notion에서 설정하기: 0~5, 총점 15, 생성형 AI 루브릭 초안 프롬프트
- Notion 폼 활용: `학생` 관계, `연결된 기사` 관계, `댓글` 텍스트, 폼 임베드 주의

3차시: 평가 DB 다시 보기와 세특 자동화 프롬프트 실습
- 평가 DB 다시 보기: 제출 여부, 기사별 댓글, 댓글 속 평가 증거
- 학생 명단 DB와 댓글 DB 연결로 미제출/참여도/평가 대기 댓글 확인
- 세특 자동화 프롬프트 만들기: AI가 대신 쓰는 것이 아니라 교사가 고쳐 쓸 초안으로 안내

주의:
- 2차시/3차시 섹션도 `insert_content`로 맨 위에 추가했다. 기존 본문과의 중복 정리는 별도 작업이 필요하다.
- 세특 프롬프트는 개인정보와 학교/교육청 지침 확인을 전제로 안내했다.

## 2026-08-22 교재 2차시 보강 · 사례 1 개편

사용자 요청으로 `노션 플랫폼을 활용한 NIE 수업 설계 — 연수 운영 페이지`
(`3c3c5f01a4a481feadb1d550dbedc406`)를 수정했다.

2차시에 세 절을 추가 (기존 1~4절 뒤, 3차시 앞):
- 5절 수업 페이지 공유와 잠금 관리 — 페이지 공유(학생은 읽기 허용), 페이지 잠금,
  링크된 데이터베이스(inbox 원본 DB) 잠금과 뷰·필터 고정
- 6절 구글폼으로 댓글 수집하기 (대안) — Notion 폼 대비 비교표 포함.
  구글폼은 학생 명단과 자동 연결이 없으므로 학생 번호 표기 규칙 통일이 전제
- 7절 DB로 쉬운 평가 — 표 뷰에서 3영역 0~5점 입력, `교사 확인 ≠ 완료` 검토 대기 뷰,
  사이드 피크로 강점·피드백·평가 포워드까지 기록
- 상단 두 운영안 표의 2차시 핵심 활동 문구도 같이 갱신했다

1차시 점검과 사례 1 개편:
- 사용자가 정리한 1차시 흐름(에듀테크 목적·한계, 지식샘터 변화와 패키지, NIE 관점,
  패키지 목표, 사례 2개)은 기존 1~8절에 모두 반영되어 있음을 확인했다
- 6절 사례 1을 "민주주의 참여 기사로 댓글 형성평가 보기"로 교체 — 보는 순서 5단계
  (기사 모음 → 댓글 원문 → 피드백 저장용 → 학생별 집계 → 분석 요약), 볼 질문 3개,
  공개 링크 추가
- 연수 전 준비 표의 사례 1 링크를 내부 링크(app.notion.com)에서 공개 사이트 링크
  (`mighty-sled-cee.notion.site/26f503723d3280d4a93aeef5ded85b4e`)로 교체 —
  연수생은 내부 링크를 열 수 없다

주의:
- 사례 1 페이지는 다른 워크스페이스(mighty-sled 공개 사이트)에 있어 MCP 통합으로
  읽을 수 없다. 내용 수정이 필요하면 해당 워크스페이스에서 직접 해야 한다.
- 사례 1 링크는 공개 URL로 넣었지만 Notion이 내부 주소(app.notion.com)로 표시할 수
  있다. 연수 전에 시크릿 창에서 링크가 열리는지 한 번 확인할 것.

## 2026-08-22 구글폼 상세 · 3차시 배포 실습 추가

같은 연수 운영 페이지에 이어서 반영했다.

2차시 6절(구글폼) 확장:
- "구글폼 작성 방법" — 양식 생성, 질문 3개(학생 번호·읽은 기사 드롭다운 필수,
  댓글 장문형) 설정 팁 표, 로그인 없이 제출 설정, 링크·QR 배포와 테스트 제출
- "응답을 Notion 댓글 DB로 연계하기" — 응답 탭 → Sheets 연결 → CSV 다운로드 →
  Notion 댓글 DB `가져오기 → CSV` → `학생`·`연결된 기사` 관계 재연결.
  재연결을 해야 제출 수 집계와 평가 흐름이 이어진다는 점을 명시
- "언제 어느 폼을 쓸까" 콜아웃 — 반복 수업은 Notion 폼, 구글폼은 시작용/일회성

3차시에 4절 "수업 페이지 제작·배포·공유 실습" 추가:
- 실습 순서 6단계 — 템플릿 복제 → 내 수업으로 바꾸기(기사·학생 ID·루브릭) →
  폼 준비 → 배포(게시/읽기 허용, 링크·QR) → 잠금 점검(2차시 5절 연계) →
  옆 선생님과 상호 공유·테스트 제출
- 배포 전 마지막 체크리스트 콜아웃(학생 링크에서 기사·폼·원본 DB 노출·집계 확인)
- 상단 두 운영안 표의 3차시 활동·결과물 문구에 실습과 "배포된 내 수업 페이지" 반영

## 2026-08-22 PRD v0.1 → v0.2 전면 개정

사용자 요청으로 PRD(`3c2c5f01a4a481b8b728e07eb284c9a4`)를 프로젝트 목적에 맞게
재정의했다. 제목도 "PRD v0.2｜생각을 나누는 뉴스 읽기 · 노션 기반 신문기사 수업
패키지"로 변경.

핵심 변경:
- 프로젝트 목적을 "① 신문기사 읽고 댓글 달기, ② 신문기사 읽고 자신의 의견
  제출하기 두 활동을 노션 플랫폼 중심으로 하는 수업 패키지 제안 + 이를 위한
  연수 자료 제작"으로 명시 (핵심 목표 콜아웃 교체)
- 제품 개요에 "패키지의 두 가지 활동" 표 추가 — 활동 A(댓글, 형성평가) /
  활동 B(의견 글, 수행평가 연계). 연수 1차시의 사례 1 = 활동 A, 사례 2(수업
  템플릿) = 활동 B로 대응을 명시
- F2를 "학생 댓글·의견 제출 (활동 A·B)"로 확장 — `활동 구분` 속성(댓글/의견)
  추가 권장, 의견 글은 "기사에 대한 생각 → 나의 생각 → 까닭" 비교 구조
- 사용자 흐름 2·8단계에 두 활동 반영, 해결하려는 문제에 "패키지+연수 자료
  필요" 항목 추가
- 실습 시나리오를 연수 운영 페이지 3차시 구성과 연동한다고 명시(링크 포함)
- 제공 패키지에 연수 운영 페이지(완료)와 성취기준 모음(미완) 추가,
  성공 기준에 "활동 A·B 중 하나를 자기 수업용으로 구성해 배포" 추가

주의:
- `활동 구분` 속성은 PRD상 권장 사항일 뿐, 실제 댓글 DB에는 아직 없다.
  적용하려면 댓글 DB에 선택 속성(댓글/의견)을 추가해야 한다.

## 2026-08-22 댓글 DB 이름 변경

사용자 요청으로 `💬 학생 댓글 · 생각 나누기` DB(`7f88a558-0743-440a-a98b-ed759275b301`)의
이름을 **`💬 학생 의견쓰기 · 생각 나누기`**로 변경했다. PRD v0.2의 활동 B(의견 제출하기)
중심 재정의와 맞춘 것. ID·스키마·뷰·폼은 그대로이고, 링크된 DB 뷰들은 자동으로 새 이름을
표시한다. 문서들에 남아 있는 "댓글 DB"라는 표현은 같은 DB를 가리킨다.

## 2026-08-22 용어 확정: 댓글 vs 의견쓰기

사용자가 두 활동의 정의를 확정했다.

- **댓글** = 학습자가 노션 페이지에 직접 자신의 의견을 작성하는 활동 (활동 A)
- **의견쓰기** = 노션 폼에서 해당 기사를 선택해 제출하면 `학생 의견쓰기` DB에
  자동으로 수집되는 활동 (활동 B)

반영한 곳:
- PRD v0.2 — 두 활동 표(활동 A/B 정의·수집 방식), 표 아래 용어 한정 문장,
  사용자 흐름 2단계, F2("학생 댓글·의견쓰기 수집")
- 연수 운영 페이지 2차시 4절 — 제목을 "Notion 폼으로 의견쓰기: 배포와 DB 관리"로
  바꾸고 용어 정리 콜아웃 추가, 본문 "댓글 수집" 표현을 "의견 수집"으로 수정

주의:
- 연수 페이지의 다른 절과 3차시, 치트시트 등에는 아직 "댓글"이 폼 수집 의미로
  섞여 있는 곳이 남아 있을 수 있다. 전면 용어 일괄 정리는 별도 작업.

## 2026-08-22 성취기준·확장 루브릭 토글 반영

사용자가 제공한 표 두 개를 연수 운영 페이지 2차시 3절(평가 루브릭) 끝,
4절 앞에 "학교급별 성취기준과 확장 루브릭" 소절로 넣었다. 토글(details) 3개:
- 📚 학교급별 핵심 성취기준 연계표 (초3-4 ~ 고등 사회, 8개 학교급/교과)
- 📏 4단계 평가 루브릭 — 7개 평가 요소 × 4~1수준, 총 28점 (제공 원문 그대로)
- 📏 5단계 평가 루브릭 — 같은 7요소를 5~1수준으로 세분(5 매우 우수 신설), 총 35점.
  5수준 기술은 4수준을 상향해 작성(맥락 파악, 교차 확인, 예상 반론, 실천 방안 등)

이로써 패키지 항목 "성취기준 모음"이 채워졌다(PRD 제공 패키지 체크 갱신 필요).

## 2026-08-22 학생 수업용 PPT 제작

연수에서 교사에게 배포할 학생 수업용 PPT를 제작했다.
`output/신문기사-댓글달기-의견쓰기-학생수업-PPT.pptx` (16:9 와이드, 16장, 발표자
노트에 교사 팁 포함, 본문 맑은 고딕)

구성:
- 표지 + 두 활동 소개(댓글달기/의견쓰기 정의 반영)
- 파트 1 활동 ① 댓글달기 × 네티켓 (6장): 댓글 하나의 힘(좋은/나쁜 예),
  네티켓 정의와 필요성, 다섯 가지 약속, 멈춰야 할 댓글 4종(악플·거짓·도배·
  저작권), 자기점검 체크리스트 5항, 연습 퀴즈(가/나/다)
- 파트 2 활동 ② 의견쓰기 4단계 (6장): 꼼꼼히 읽기(육하원칙), 글쓴이 생각과
  내 생각 비교(찬성/반대/새 생각), 까닭 문장 틀과 까닭 3원천, 의견 글 4문장
  구조+예시, 노션 폼 제출 4단계와 주의
- 마무리: 평가 3영역(기사 이해·생각 연결·생각 표현) + 오늘의 다짐

검증: validate.py 통과, LibreOffice 렌더링으로 16장 전수 시각 QA 완료.

## 2026-08-22 PPT 보강 · 백워드 설계 삽입

학생 수업용 PPT(17장으로 확장):
- 파트 2에 15번 슬라이드 "우리 글은 이렇게 평가해요 — 수행평가 기준" 추가.
  3영역(기사 이해·생각 연결·생각 표현) 각 0~5점·총 15점을 학생 언어로 풀고,
  영역별 "5점의 모습"과 평가 포워드 안내 포함. 의견 글 완성(14)과 폼 제출(16) 사이.
- 곰곰이쌤 마스코트 삽입: 표지(bear_wink + "곰곰이쌤" 라벨), 마무리 다짐 바(gomgom-bear).
- 재검증·전수 렌더링 QA 완료. output/ 동일 파일명으로 갱신.

연수 운영 페이지 1차시에 백워드 설계 삽입:
- 4절(성취기준) 끝, 5절 앞에 "성취기준에서 수업으로 — 백워드 설계(이해중심
  교육과정)" 소절 추가. 위긴스·맥타이 UbD 3단계(바라는 결과 확인 → 수용 가능한
  증거 결정 → 학습 경험과 수업 계획) 표에 이 패키지의 대응(성취기준·핵심 질문 /
  댓글·의견 글 증거와 루브릭 / 활동 A·B)을 매핑. "왜 백워드인가" 콜아웃으로
  교육과정-수업-평가(기록) 일체화와의 연결을 설명.

## 2026-08-22 참고 도서 목록 추가

1차시 백워드 설계 소절 아래(콜아웃 뒤, 5절 앞)에 "📚 참고 도서" 목록을 추가했다.
- 프레이·피셔 『피드백, 이렇게 한다』(강정임 옮김, 교육을바꾸는사람들, 2021,
  원제 The Formative Assessment Action Plan) — 사용자 지정 도서
- 위긴스·맥타이 『Understanding by Design』(ASCD), 맥타이·위긴스 『핵심 질문』
  (사회평론아카데미), 김경자·온정덕 『이해중심 교육과정: 백워드 설계』(교육아카데미
  2014), 온정덕 외 『교실 속으로 간 이해중심 교육과정』(살림터 2018), 강현석 외
  『백워드 설계의 이론과 실천: 교실혁명』(학지사 2021) — 리서치로 확인해 추가

## 2026-08-22 참고 도서 링크 · 피드업/피드백/피드포워드 추가

- 참고 도서 목록에 YES24 상품 링크를 붙였다: 피드백 이렇게 한다(101807156),
  핵심 질문(35091287), 교실 속으로 간 이해중심 교육과정(67268360), 백워드
  설계의 이론과 실천(97783725). UbD 원전은 YES24 미취급 표기, 김경자·온정덕
  『이해중심 교육과정』은 YES24 미취급이라 알라딘 링크로 대체.
- 백워드 설계 소절과 참고 도서 사이에 "배움을 잇는 세 가지 질문 — 피드업·
  피드백·피드포워드" 소절 추가. 해티·팀펄리의 세 질문(어디로/지금 어디/다음
  어디)을 표로 정리하고, 패키지의 `강점`·`피드백`·`평가 포워드` 세 속성 및
  학생 PPT 수행평가 기준 슬라이드와의 대응을 명시. 백워드(평가 먼저 설계)와
  한 세트(평가를 배움으로 되돌림)라는 콜아웃 포함.

## 2026-08-22 피드업·피드백·피드포워드 정의 확정 반영

사용자가 세 개념의 정의를 확정했다.
- 피드업 = 성취기준(수업 및 평가의 목표)을 활동 전에 공유
- 피드백 = 학생의 동료평가 · 자기평가 · 교사평가
- 피드포워드 = 평가 결과로 성취수준 도달 상태를 파악해 다음 수업 단계 고려

반영한 곳:
- PRD F6을 "학생 피드백 체계 (피드업·피드백·피드포워드)"로 확장 — 세 단계
  정의를 명시하고, 기존 피드백 작성 원칙(잘한 점→근거→보완점 등)은
  "교사 피드백 작성 원칙"으로 유지
- 연수 운영 페이지 1차시 "배움을 잇는 세 가지 질문" 표의 피드업(성취기준
  공유)·피드백(동료/자기/교사평가 세 축)·피드포워드(도달 상태 파악 → 다음
  수업 단계) 행을 확정 정의로 갱신

주의:
- 동료평가(친구 댓글에 질문·보완 의견)는 현재 DB 속성으로는 지원되지 않고
  페이지 댓글/수업 활동으로 하는 것을 전제로 안내했다. 속성으로 관리하려면
  의견쓰기 DB에 동료평가 필드를 추가해야 한다.

## 2026-08-22 3차시 5절 "새로운 신문기사 수업을 위한 페이지 관리" 추가

3차시 4절(배포 실습) 뒤에 5절을 추가했다. 사용자가 정의한 페이지 관리 방식:
1. `NIE 수업` 상위 페이지 생성 (신문기사 수업을 모두 이 아래에)
2. 신문기사 수업 템플릿 복제
3. 복제본에서 기사 전부 삭제 → 빈 구조의 샘플 페이지화 (잠금해 원본 보존)
4. 새 수업마다 샘플 복제 후 새 기사만 등록 (이름 규칙: 날짜·기사 주제)

구조 예시 트리와 주의 콜아웃(페이지 복제 시 DB도 복제됨 — 학생 명단 DB는
학급당 하나 유지, 복제 후 폼의 `학생` 관계와 중복 명단 확인) 포함.
상단 두 운영안 표의 3차시 활동에 "새 수업을 위한 페이지 관리"를 추가했다.

## 2026-08-22 이번 달 수업 안내 토글 추가

연수 운영 페이지 맨 하단에 "📅 이번 달 수업 안내 — 오늘 연수의 다음 단계"
토글을 추가했다. 두 후속 강좌 안내:
1. 자동화 평가 시스템 기초 (노션 × 클로드 대시보드) — 수업 결과 DB로 세특
   초안·피드백까지 자동 생성, 교사 확인 후 전달. 3차시 세특 프롬프트의 확장
2. 질문하는 수업을 위한 챗봇 제작 및 평가 활용 — 수업용 챗봇 설계·실습,
   대화 기록의 형성평가 연계

세부 일정·신청 방법은 추후 안내로 표기. 확정되면 토글 안에 채워 넣을 것.

## 2026-08-22 평가 루브릭 작성 스킬 (edu-rubric) 제작

「신문기사 댓글쓰기 평가 루브릭」(3c2c5f01a4a48065bebff764fd39e1d3) 형식을
기준으로 루브릭을 만드는 스킬을 작성했다.
- 파일: `skills/edu-rubric/SKILL.md`
- 노션 저장본: 「스킬 · 평가 루브릭 작성 (edu-rubric)」
  (`3c4c5f01a4a4812fbe67d861c1bd6399`, 워크스페이스 개인 페이지로 생성 — 위치는
  사용자가 옮기면 됨)

산출물 구조 7블록 고정(성취기준 콜아웃 → 도입 → 영역×4수준 표 → 영역별 해설
[낮은/높은 예시 + AI 평가 증거 + AI 지시문] → 학생용 체크리스트 → 교사 모범
예시 2+1개와 증거 표 → 수업 활용 팁). 품질 규칙(입장 아닌 근거 평가, 관찰
가능 증거, 부사만 바꾼 기술문 금지, 교사/학생 언어 분리, AI 안전 규칙)과
척도 변형(기본 4수준, 0~5형, 5수준형, 7요소 확장형) 포함.

참고: 이 커밋부터는 사용자 합의에 따라 로컬 커밋만 하고, "마무리/끝" 신호가
오면 한 번에 푸시한다.

## 2026-08-22 교원 연수 교안 PPT 개정판 (60p → 68p)

사용자 확정본(edu-training-deck 형식 60장, 「노션을 활용한 NIE 조사학습을 위한
학생 참여형 수업 설계」)의 디자인 틀·차시 흐름은 그대로 두고, 연수 페이지에
새로 정리한 내용을 같은 레이아웃 복제 방식으로 8장 추가 + 1장 갱신했다.
`output/노션NIE-교원연수교안-개정판-68p.pptx`

추가 슬라이드: 13(두 가지 활동 — 댓글 vs 의견쓰기), 15(백워드 설계 3단계),
41~43(2차시: 공유·잠금 관리 / 구글폼 대안+DB 연계 / DB로 쉬운 평가),
55~56(3차시: 피드업·피드백·피드포워드 확정 정의 / 3영역 평가 루브릭),
62(새 수업을 위한 페이지 관리 — 샘플 복제).
갱신: 21장 댓글 예절 → 네티켓 다섯 가지 약속.
검증: validate --original 통과, 신규·수정 슬라이드 전수 렌더링 QA 완료.

## 2026-08-22 교사 참여형 평가 안내 추가 (3차시 맨 앞)

연수 운영 페이지 3차시 표지 바로 아래(1절 앞)에 "교사 참여형 평가 — 교사도
평가의 '참여자'로" 소절을 넣었다. 네 가지 방식(교사도 함께 쓴다·기준을 함께
만든다·수업 중 실시간 참여·연수 실습 교사 선평가→비교)과 효과 표, 핵심
콜아웃(교사는 첫 번째 독자이자 함께 쓰는 사람 — 동료·자기·교사평가가 한
흐름) 구성. 처음에 2절 뒤에 넣었다가 사용자 요청으로 3차시 맨 앞으로 이동,
오타(큼다→큽니다)도 수정.

교안 PPT도 3차시 개요 바로 뒤(48번)에 "3차시 · 교사 참여형 평가 안내"
슬라이드를 추가해 69장이 됐다. 파일명 갱신:
`output/노션NIE-교원연수교안-개정판-69p.pptx` (68p 파일 삭제).
주의: python-pptx 저장 시 슬라이드 파트명이 순서대로 재부여되므로, 이후
add_slide로 특정 슬라이드를 복제할 때는 파일명이 아니라 현재 위치 기준으로
확인할 것.

## 2026-08-22 수업도우미 PRD v0.1 작성

새 제품 「과정 중심 평가를 위한 수업도우미」(PC용 HTML 웹앱)의 PRD를
노션에 작성했다: 「PRD v0.1｜과정 중심 평가를 위한 수업도우미」
(`3c5c5f01a4a481bb9f81d079445e3525`, 워크스페이스 개인 페이지 — 위치는
사용자가 옮기면 됨)

근거 자료(둘 다 사용자 워크스페이스에서 확인):
- 「[Skill]신문기사 댓글 AI 자동 채점」(87d5130c...) — 승인 필수 규칙,
  루브릭 확정 5지침, 3영역×4수준 12점, 성취수준 분포(10~12/7~9/4~6/1~3),
  교사·수식 속성 기록 금지, 예외 처리, 평가 결과 페이지 6단계
- 「신문기사에 대한 자신의 의견 제시하기(템플릿)」(b25c5f01...) — 기사
  자료실 · 평가 결과 관리 DB(519c5f01...) · 학생 명단 DB 구조

PRD 골자: 4단계 흐름(①평가 설계: 학년·과목(국어·사회 우선)·성취기준 선택
+ 교사 코멘트 → 루브릭·성취수준 점수표 생성·편집·승인 → ②자동 채점·세특
피드백 → ③교사 검수(점수 가감·조정 이유 필수) → ④엑셀/시트 정리표·평가
포워드 모음·성취기준별 도달/미도달 분석표). 노션 연계는 (a)노션 연결 /
(b)평가결과 정리 2모드, MVP는 b. 로드맵 M1~M5, 미결정(AI 호출 방식·시트
연동·기본 척도) 명시.

## 2026-08-23 수업도우미 개발 시작 — CSV 검수 모듈 v0.1

사용자 결정: 평가 결과를 노션에서 바로 받을 수 없는 경우 CSV 파일을 받아
검수한다 (PRD F3-1로 명세 추가). "개발 시작하자" 지시로 1차 산출물 제작.

`webapp/수업도우미-검수.html` — PC용 단일 파일, 외부 의존성 없음.
- ① CSV 업로드(드래그&드롭/선택/붙여넣기, UTF-8·EUC-KR 자동, 탭 구분 인식)
- ② 열 자동 매핑(노션 내보내기 열 이름 유사도 매칭 + 수동 수정, '교사' 열은
  AI 매핑에서 제외) · 척도 선택(1~4 총12점 기본 / 0~5 총15점, 자동 감지)
- ③ 검수 테이블: AI 점수 표시, 교사 점수(기본=AI) 가감 시 빨간 표시,
  조정 이유 미입력 경고, 총점·성취수준 자동, 피드백·평가 포워드 편집,
  우선 검토 플래그(영역 점수차≥2 또는 15자 미만 글), 검수 완료 체크,
  localStorage 자동 저장 + JSON 저장/불러오기
- ④ 내보내기: 검수 결과 CSV(BOM, 엑셀 호환), 학급 분석표 CSV(평균·분포·
  조정 건수·부족 영역·보통 이하 명단·다음 수업 제안), 평가 포워드 복사
- AI 점수 열이 없는 CSV(구글폼 등)는 수기 채점 모드로 동작

검증: Playwright(chromium)로 스모크 테스트 — 샘플 로드, 자동 매핑, 6행
렌더, 조정 경고 표시/해제, CSV·분석표 내용 검증, 스크린샷 시각 확인 통과.
다음: M1 평가 설계(성취기준 데이터 + 루브릭 생성), AI 채점 연동(호출 방식
미결정 — PRD 10절).

## 2026-08-23 CSV 검수 모듈 v0.2 — 미제출·모드 선택 반영

사용자 피드백 반영:
1. 학생 명단(선택) 입력 추가 — 범위(S001-S030)·쉼표·줄바꿈 인식. 명단이
   있으면 요약 카드에 미제출 인원·명단이 뜨고, 학급 분석표 CSV의 맨 위에
   학급 명단 인원 → 미제출 인원 → 미제출 학생이 온다.
2. 열 매핑 단계에 방식 선택 카드 추가 — (a) 노션 연동(내보내기 CSV의 AI
   점수·피드백 검수 후 노션 재반영) / (b) 연동 없이 바로 평가(학번·글만으로
   루브릭 기준을 보며 교사가 직접 채점). 파일에 AI 점수 열이 있으면 (a),
   없으면 (b)를 자동 추천. (b)에서는 검수 화면의 기본 루브릭 패널
   (3영역×4수준, 스킬 원문 기반)이 자동으로 펼쳐진다.

Playwright 재검증: 추천 배지, 미제출 2명 계산, 분석표 상단 순서, (b) 모드
수기 채점·루브릭 자동 펼침 확인.

## 2026-08-23 수업도우미 M1 — ⓪ 평가 설계 화면 추가

`webapp/수업도우미-검수.html`을 통합 앱으로 확장(헤더: "수업도우미 — 과정
중심 평가", 단계 표시줄에 ⓪ 설계 추가).

⓪ 평가 설계:
- 성취기준 데이터 내장 — 국어·사회, 학년군 4개(초3-4/초5-6/중/고), 사용자
  제공 성취기준 연계표 기반. STD_DATA 객체에 과목 추가만 하면 확장.
- 학년군·과목 → 성취기준 체크 선택 → 수업 코멘트 입력.
- [기본 루브릭 만들기]: 학년군별 영역 세트(초3-4: 이해·연결·표현 / 초5-6:
  요약·연결·주장 / 중: 요약·신뢰성·주장 / 고: +시민적 관점)에 규칙 적용 —
  성취기준에 신뢰/출처/공정 키워드가 있으면 영역②를 '출처와 근거의 신뢰성
  판단'으로 교체, 해결/사회문제/시민이면 시민적 관점 추가(최대 4영역).
  모든 셀(영역명·4수준 기술문) 직접 편집 가능. 성취수준 점수표(N영역×4점,
  구간·상태·지도 방향) 자동 생성.
- [AI 프롬프트 복사]: API 없이 쓰는 보조 모드 — 성취기준·학년군·코멘트·작성
  지침(스킬의 루브릭 확정 지침 요약)을 담은 프롬프트를 클립보드로. 챗봇
  결과(마크다운 표)를 [AI 결과 붙여넣어 가져오기]로 파싱해 편집기에 반영.
- [승인]: 승인 일시 기록, ③ 검수 화면의 채점 기준 패널이 기본 루브릭 대신
  승인된 루브릭으로 교체 표시. localStorage(별도 키)로 설계 상태 유지.

Playwright 검증: 성취기준 렌더, 신뢰 키워드 영역 교체, 점수표, 승인→검수
패널 반영, 프롬프트 내용, 붙여넣기 파서, 기존 CSV 검수 회귀 모두 통과.
남은 것(M2+): API 키 직접 호출 모드(사용자 결정 대기), 노션 API 연동(a).

## 2026-08-23 수업도우미 M2 — Claude API 직접 호출 (AI 자동 채점)

사용자 결정: AI 연동은 "API 키 직접 입력" 방식(PRD 10절 미결 항목 해소).
`webapp/수업도우미-검수.html` v0.4.

🔑 Claude AI 연결 카드 (맨 위):
- 키는 입력 즉시 localStorage(별도 키 sudoumi_api_v1)에만 저장 —
  **HTML 파일·깃허브·노션 어디에도 키가 들어가지 않는다**(코드에는 입력칸만
  있음). 공용 PC용 [키 지우기], 앱 내 경고문 포함. 진행상태 .json 내보내기에도
  키는 포함되지 않음.
- 모델 선택: claude-opus-5(기본) / claude-sonnet-5 / claude-haiku-4-5.
- 호출: fetch → api.anthropic.com/v1/messages, anthropic-version 2023-06-01,
  anthropic-dangerous-direct-browser-access 헤더(브라우저 CORS).
  opus-5에는 server-side-fallback-2026-07-01 베타 + fallbacks:"default" 적용
  (안전 거절 시 자동 대체 모델). stop_reason refusal·401·429 안내 처리.

⓪ [✨ AI로 루브릭 만들기]: 기존 프롬프트 빌더 재사용 → 응답 표 파싱 →
편집기 반영(키 없으면 버튼 숨김, 복사 방식은 그대로 유지).

③ [🤖 AI 자동 채점 + 피드백·세특 초안]:
- 승인 루브릭 필수(없으면 confirm 후 기본 3영역 루브릭 사용 — 스킬의
  "승인 없이 채점 금지" 원칙 유지). 4영역 루브릭은 앞 3영역으로 채점 안내.
- 검수 완료 아닌 미채점 행만 순차 채점(재실행 시 실패분만 재시도),
  진행 표시. 학생 글+기사 제목만 전송(**학번 미전송** — 개인정보 최소화).
- 응답 JSON: scores/근거/피드백/평가포워드/세특초안/확신도. 교사 점수가
  비어 있으면 AI 점수로 채우고, 이미 입력한 값은 보존. 확신도 "낮음" →
  ⚠ 우선 검토 플래그. 채점 규칙(입장 아닌 근거 평가, 추측 금지, 짧다고만
  감점 금지)을 시스템 프롬프트에 명시.
- 검수 표에 "세특 초안" 열 추가(편집 가능), 검수 결과 CSV에 세특 초안·
  AI 채점 근거 열 추가.

Playwright(API 모킹) 검증: 키 저장/표시, AI 루브릭 생성→승인, (a) 미채점
행만 채점, (b) 전체 채점+기본 루브릭 confirm, 교사 점수 자동 제안, 확신도
낮음 플래그, 학번 미전송, CSV 세특 열 — 전부 통과. 실 API 호출은 사용자
키로 확인 필요.

## 2026-08-23 수업도우미 v0.5 — Gemini 무료 키 지원 (제공자 선택)

사용자 질문("제미나이 키는 무료 아니야?")에 따라 AI 연결을 제공자 선택형으로
확장. Claude를 교체하지 않고 **Gemini · Claude 둘 다 지원**.

- 제공자 선택: Gemini(기본, 무료 티어 가능 — 실습생 배포용) / Claude(유료,
  채점 품질·데이터 비학습 — 실제 평가용). 선택에 따라 모델 목록·키 발급
  안내·placeholder가 바뀜.
- Gemini 모델: gemini-2.5-flash(기본)/flash-lite/pro. REST
  generativelanguage.googleapis.com v1beta generateContent, flash 계열은
  thinkingConfig.thinkingBudget=0으로 비용·지연 절약. 400/403/429 안내 처리.
- 앱 내 고지: 무료 티어는 입력이 구글 모델 개선에 활용될 수 있음 →
  실습·샘플용, 실제 학생 글은 유료 티어/Claude 권장.
- 기존 sk-ant 저장분은 자동으로 Claude 제공자로 인식(마이그레이션).
- 호출부는 callAI()로 통합(루브릭 생성·자동 채점 공용). 학번 미전송 동일.

Playwright(모킹) 검증: 기본 Gemini·모델 목록·힌트, 키 저장 상태, Gemini
루브릭 생성→승인→b모드 6/6 채점, 요청 URL·thinkingBudget·학번 미전송,
Claude 전환 시 모델 목록 교체, 구버전 저장 마이그레이션 — 전부 통과.
실키 확인은 미실시(사용자 키 필요).

배포 안내 방침(사용자와 논의): 실습생에게는 파일(QR)+상시 링크로 배포,
Gemini 무료 키 발급(aistudio.google.com)을 사전 과제로, 키 없는 참가자는
프롬프트 복사 방식이 플랜 B.

## 2026-08-23 챗봇 교안 — Gemini 무료 API 안내 페이지 추가

「💬 질문하기 수업을 위한 챗봇 제작 및 평가 활용 수업안」
(3c5c5f01a4a48158b79df2188dbb0c91, 다른 세션에서 제작된 교안) 하위에
「🔑 Gemini 무료 API 발급 방법과 API 이해하기」
(3c5c5f01a4a4818da9fde8f444d86bf3) 생성, 본문 '수업 자료' 콜아웃에 링크 추가.

구성: ① API 개념(식당 주문 비유 표 + 챗봇 사이트 vs API 비교표)
② 무료 키 발급 5단계(aistudio.google.com, 카드 불필요, 학교 워크스페이스
계정 차단 가능 → 개인 계정 안내) ③ 무료 티어 범위·한도·데이터 활용 경고
(실습용 OK, 실제 학생 글·개인정보 금지, 유료/Claude 안내) ④ 키 보안 수칙
3가지 ⑤ FAQ 토글 4개(과금 여부, 키 분실, 챗봇 사이트와 차이, Claude 키)
⑥ 오늘 할 일 정리 콜아웃. 무료 티어 수치는 변동 가능성이 있어 구체 숫자
대신 'AI Studio 요금 안내 확인'으로 표기.

## 2026-08-23 [4국02-02] 중심 생각 신문기사 수업 세트 (노션)

연수 운영 페이지(3c3c5f01a4a481feadb1d550dbedc406) 하위에
「📰 중심 생각 신문기사 수업 세트 [4국02-02]」(3c5c5f01a4a48184aac7c30fdf5c0e57)
생성. 수업 흐름 5단계(기사 읽기→중심 문장 밑줄→문단 요약→전체 간추리기→
의견쓰기 폼 제출) + 의견쓰기 폼·DB 연결 방법 + 평가 포인트 포함.

하위 기사 3편(모두 학습용 창작 기사 — 신문 형식: 제호·부제·기자명(가상)·
리드가 곧 전체 요약 모범답, 문단 구조 문제→원인→노력→결과→의미, 문단별
중심 문장 1개 설계):
- 🍚 급식실 남은 음식, 석 달 만에 '절반'으로 (3c5c5f01a4a481b3bd28f493134f6dc1)
- 🤖 학교 도서관에 '책 찾아 주는 로봇' 왔다 (3c5c5f01a4a481bc8315c4e89210e5f5)
- 🥛 버려지던 우유팩, 화장지로 다시 태어났다 (3c5c5f01a4a481398826dbed6868fed6)

각 기사 페이지: 기사 본문 + 학생 활동지(문단 요약 빈칸 표, 리드 비교,
의견쓰기 연결 발문) + 교사용 정답표 토글(문단별 중심 생각·뒷받침 내용·
전체 요약 모범답·타 성취기준 확장). 교사용 정답이 있으므로 학생 배포 시
기사 본문만 뉴스 읽기 자료실 DB로 복사하도록 안내문에 명시.

## 2026-08-24 질문 도우미 챗봇 수정 사양서 (반·번호 + 미제출)

사용자 요청: 챗봇 온보딩의 '모둠'을 반·번호로 바꾸고, 보고서에 미제출
학생을 찾아 넣기. **챗봇 소스는 이 저장소에 없음**(별도 question-bot 폴더,
question-bot-amber.vercel.app 배포)이라 직접 수정 불가 →
`docs/챗봇-수정사양서-반번호-미제출.md`로 Claude Code 투입용 사양서 작성.
(선생님 워크플로: .md 사양서 → claude → vercel --prod)

내용: 데이터 구조(LS_PROFILE/LS_LOGS/LS_ROSTER, cls·num, sid/label 헬퍼),
기존 모둠 기록 마이그레이션 코드(group→cls, 기록 보존), 온보딩 화면
(반 select + 번호 44px 그리드 + 별명, 직전 반 기억), 보고서 상단
학급 설정바 + missingStudents() + 참여율·미제출 번호 카드·복사 버튼,
문구 일괄 교체표, 테스트 케이스 12개, 배포 절차.
개인정보 원칙(실명 없음·기기 밖 미전송) 유지.

미제출 계산·마이그레이션 로직은 node로 단위 검증(문자열/숫자 혼재 처리 확인).
남은 것: 사용자가 question-bot 폴더에서 적용·배포 후 실제 링크 테스트.

## 2026-08-25 챗봇 운영 방식 결정 — 연수안 반영

사용자 결정(추천 채택): 수업 운영 = 질문 도우미 웹앱(노션 결합, 본선) /
챗봇 제작 체험 = 복사형 프롬프트(3차시 실습) / 구글 앱스 스크립트 방식은
미채택(접근 코드·intro 화면 패턴은 이미 웹앱에 반영돼 있음을 확인).
GAS 챗봇 원본은 네트워크 차단으로 내부 코드 미확인 — 비교는 일반 특성 기준.

노션 반영:
- 3차시 페이지(c0c7142fe480410196184dbb739fd271)에 "0. 챗봇 운영 방식
  안내" 섹션 삽입 — 원칙 콜아웃, ①웹앱/②프롬프트 용도별 비교표,
  연수생 안내 문구 예시, GAS 참고 콜아웃.
- 운영 관리 메모(3c5c5f01a4a481fc8d7ed48503ebdc08) 완료 목록에 결정 기록.

별도 진행 중: PR #10 (claude/vocabulary-dictionary-support) — 어휘 응답
2단계(사전 뜻만 → 문맥은 이어 물을 때) + 내장 사전 103어 + 교사 낱말 등록
UI. 48회기 192턴 회귀 통과. 초대 문장은 사용자 요청으로 제거.

## 2026-08-25 1회성 챗봇+세특 통합 프롬프트 (연수 ② 실습 자료)

사용자 질문(설치 없이 되는가?)에 대한 결론: 클로드 코드·Node·CLI는
HTML 배포형(C)에만 필요. 웹 챗봇 무료 계정만으로 되는 A 수준(프롬프트만)
완성본을 제작 — `docs/1회성-챗봇-세특-프롬프트.md`.

구조: 빈칸 4개(성취기준/지문/핵심 낱말/교사 암호) → 학생 챗봇 모드
(V2 규칙 10개: 답 먼저, 한 턴 한 동작, 어휘 2단계, 대필 거절, 개인정보
차단, 지어내지 않기 등) → 교사가 "평가 시작+암호" 입력 시 같은 대화에서
평가 모드 전환(요약·4요소 채점표·세특 초안·다음 지도·AI 초안 고지).
학생의 "평가 시작" 장난은 암호 없으면 무시. 근거 발화 없는 요소는
"관찰 기록 없음"(추측 채점 금지).

B교사 가상 시나리오로 6개 규칙 작동 검증(문서 내 표). 주의사항 명시:
만 14세 미만 계정 제한 → 교사 시연·모둠 태블릿용, 학급 전체는 웹앱.
노션 챗봇 교안 하위에 복사용 페이지 추가 예정.
