# 교사용 경량 질문챗봇 배포앱 PRD

## 1. 한 줄 정의

교사가 코드를 수정하지 않고 자신의 API, 수업 목표, 성취기준, 평가기준, 평가 근거와 수업자료를 입력한 뒤, **공통 웹 챗봇과 같은 대화 엔진·화면 구조·학습 흐름**으로 수업을 운영하고 학생 응답과 평가 결과는 교사 소유 Google Sheet에서 관리하는 연수용 개인 설정 앱이다.

여기서 `경량`은 챗봇 기능을 줄인다는 뜻이 아니다. 실제 웹 챗봇의 대화 정책과 학생 경험은 유지하고, 교사마다 달라야 하는 `API 연결·수업 설정·결과 저장`만 작은 배포본으로 분리한다는 뜻이다. 연수 화면에서는 혼동을 줄이기 위해 제품명을 **내 수업 질문챗봇**으로 표시한다.

### 현재 구현 현황

| 단계 | 상태 | 남은 일 |
| --- | --- | --- |
| 0. PRD·경량 Apps Script 클라이언트 구조 | 완료 | — |
| 1. 교사 설정 마법사 | 코드 완료 | 실제 Google Sheet 사본에서 초보 교사 사용성 검수 |
| 2. 학생 세션·개인 Sheet 기록 | 코드 완료 | 실제 Apps Script 동시 기록 검수 |
| 3. 기존 챗봇 공통 엔진 | 공통 코어·동일 앱 경로 연결 완료 | 기존 `newscomment-ai` 프로덕션 복구·브랜치 병합·연결키 설정 후 실서비스 확인 |
| 4. 개인 API 모델 호출 | 코드 완료 | 교사 테스트 API로 실제 응답·사용량 검수 |
| 5. 교사 평가 검수 | 1차 기능 완료 | 1회성 세특·종합 세특·학기 누적 성장 보드는 2차 후속 |
| 6. 배포 안내·운영 보호 | 코드 완료 | 교사 사본별 최초 웹앱 배포와 실제 학교 정책 확인 |
| 7. 연수 실증 | 미착수 | 초보 교사 3명·25명 동시 제출·30명 분산 제출 실증 |

개인 설정용 Apps Script 프로젝트는 기존 v37 배포본과 분리하지만, 대화 판단 서버를 새로 만들지는 않는다. 기존 질문중심 챗봇 앱 `https://newscomment-ai.vercel.app` 안의 `/api/lite-engine/plan`과 `/api/lite-engine/finalize`가 웹 챗봇과 같은 `questioning-dialogue-v2` 공통 코어를 호출한다. 현재 저장소 기준으로 1차 기능 코드는 완성했지만, 기존 프로덕션 복구·기능 브랜치 병합·연결키 설정·교사 사본별 웹앱 배포·실제 API·25명 동시 제출 및 30명 분산 제출 검증이 끝나기 전에는 **연수 현장 배포 승인**으로 표시하지 않는다.

2026-09-10 운영 점검에서 `https://newscomment-ai.vercel.app/`, `/questioning-chatbot`, `/api/health`가 모두 HTTP 500을 반환했다. 로컬에서도 `NEXT_PUBLIC_SUPABASE_URL`과 `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`가 없을 때 같은 오류를 재현했으므로, Production 환경변수와 런타임 로그를 먼저 확인한다. 이는 원인 후보이며 운영 로그를 확인하기 전에는 환경변수 누락으로 확정하지 않는다. 기능 브랜치가 아직 `main`에 병합되지 않은 상태에서는 두 lite 경로도 운영 중으로 간주하지 않는다. 최신 배포 대상과 확인 근거는 [배포 실행 기록](LIGHTWEIGHT_APP_DEPLOYMENT.md)을 따른다.

Google Sheet 사본에는 바운드 Apps Script가 함께 복사되지만 웹앱 배포 주소까지 자동으로 생기지는 않는다. 따라서 연수생 사본마다 최초 1회 `배포 → 새 배포 → 웹 앱`과 Google 권한 승인이 필요하다. 코드를 편집하는 단계는 아니며, 이 제약을 없애려면 후속 버전에서 중앙 학생 웹앱과 교사별 인증·저장 연결 구조로 바꿔야 한다. [Google 바운드 스크립트](https://developers.google.com/apps-script/guides/bound), [Apps Script 배포](https://developers.google.com/apps-script/concepts/deployments)

Apps Script 웹앱을 배포 사용자로 실행할 때 Google의 사용자당 동시 실행 상한은 30이므로, 교사 대시보드나 자동 재시도가 함께 실행될 여유를 남기기 위해 1차 버전은 동시 제출 25명 이하를 권장한다. 30명 학급은 15명씩 두 모둠으로 나누어 약 10초 간격으로 입장·첫 질문을 시작한다. [Apps Script 할당량](https://developers.google.com/apps-script/guides/services/quotas), [웹앱 실행 주체](https://developers.google.com/apps-script/guides/web)

## 2. 배경과 해결할 문제

연수 1차시에는 강사가 경량 교사앱에서 미리 완성한 학생용 질문챗봇을 공통 실습본으로 사용한다. 연수생은 아직 개인 API나 수업 설정을 다루지 않고 학생 역할로 먼저 참여해 질문, 근거 확인, 생각 수정, 대화 종료와 평가 기록의 흐름을 경험한다. 이후 같은 경량앱의 교사 설정 화면을 열어 방금 체험한 챗봇이 어떻게 구성되었는지 확인하고, 2차시부터 각자 사본에서 자기 수업용 챗봇을 만든다.

기존 전체 Apps Script를 복사하게 하면 코드가 크고 설정 과정이 복잡하며, 챗봇 정책을 고칠 때 모든 교사 사본을 다시 수정해야 한다. 반대로 1차시 체험용과 개인 제작용을 서로 다른 챗봇으로 운영하면 배운 경험의 전이가 끊긴다. 따라서 강사의 공통 실습본과 연수생의 개인 제작본은 모두 같은 경량앱과 공통 엔진을 사용하고, 수업 설정과 저장 위치만 달라지게 한다.

경량앱은 다음 세 가지를 분리한다.

1. **교사 소유 영역**: 개인 API 키, 수업·평가 설정, 학생 응답, 교사 최종 판단
2. **기존 챗봇 중앙 관리 영역**: 대화 국면, 질문 정책, 안전 규칙, 프롬프트 버전
3. **학생 공개 영역**: 반-번호 입력과 질문·답변 화면

### 2.1 공통 웹과 맞추는 것과 교사별로 달라지는 것

| 구분 | 공통 웹과 맞추는 계약 | 교사별로 분리 |
| --- | --- | --- |
| 대화 | 자료 기반 답변, 안전 처리, 종료 처리, 1·2국면, 관리 질문, 평가/자료 탐색 모드 정책 | 개인 API의 사용량과 호출 권한 |
| 화면 | 지문과 대화의 2열 구조, 모바일 탭, 반-번호 입장, 빠른 질문, 대화 마치기 | 챗봇 이름, 교과, 학년, 수업명, 시작 질문 |
| 평가 | 동일한 루브릭 자료 구조와 자동 판단용 관찰값 | 교사의 성취기준·평가기준·수준 기준·최종 판단 |
| 저장 | 같은 학생 발화→챗봇 응답 흐름 | 공통 운영본은 중앙 저장, 개인 운영본은 교사 Google Sheet 저장 |

따라서 연수생은 1차시와 개인 운영 단계에서 서로 다른 챗봇을 새로 배우지 않는다. 같은 학생 경험을 유지한 채 운영 주체만 `강사 설정 → 교사 개인 설정`으로 바뀐다.

## 3. 제품 목표

- 교사가 2차시 안에 자신의 수업을 설정하고 미리보기까지 마친다.
- 교사가 코드 편집기나 시트 열 구조를 몰라도 사용할 수 있다.
- 1차시에서 본 학생 화면·대화 방식이 개인 설정 후에도 그대로 이어진다.
- 백워드 설계 순서인 `목표 → 성취기준 → 평가기준 → 평가 근거 → 수업자료 → 운영 모드`를 화면이 안내한다.
- API 키는 교사 소유 Apps Script의 Script Properties에만 저장한다.
- 학생 대화와 평가 결과는 교사 소유 Google Sheet에만 저장한다.
- 기존 질문중심 챗봇의 공통 정책을 갱신해도 교사 사본을 다시 배포하지 않도록 한다. 단, 연결키 회전이나 API 계약 변경 때는 사본을 업데이트한다.
- 자동 판단은 교사의 최종 평가가 아니라 검토 초안으로 표시한다.

## 4. 비목표

- 학교 계정의 OAuth·관리자 승인을 자동으로 우회하지 않는다.
- 학생 이름, 이메일, 전화번호를 수집하지 않는다.
- 기존 질문중심 챗봇 서버에 교사의 API 키나 학생 대화 원문을 장기 저장하지 않는다.
- 1차 버전에서 PDF·Word·이미지 평가까지 한꺼번에 구현하지 않는다.
- 1차 버전에서 여러 수업을 동시에 운영하지 않는다. 한 사본은 한 활성 수업을 기본으로 한다.

## 5. 연수 흐름

연수는 `강사가 미리 제작한 학생용 챗봇으로 1차 체험 → 학생 화면 뒤의 설정 구조와 평가 근거 저장·검수 흐름 이해 → 연수생 각자가 배포받은 경량 교사앱에서 자기 수업 챗봇 제작` 순서로 진행한다. 1차시에는 연수생의 개인 API 연결이나 개인 챗봇 제작을 요구하지 않는다.

| 차시 | 교사 경험 | 사용 제품 | 완료 결과 |
| --- | --- | --- | --- |
| 1차시 | 강사가 경량 교사앱으로 미리 만든 학생용 챗봇에 연수자가 학생 역할로 참여한다. 질문·힌트·생각 수정·종료를 체험한 뒤, 강사가 설정 화면과 결과 Sheet를 열어 학생 활동이 평가 근거로 남는 과정을 보여준다. 이 차시에는 연수생의 개인 API 연결이나 개별 챗봇 제작을 요구하지 않는다. | 강사 경량앱의 완성된 학생용 챗봇 + 강사 결과 Sheet | 학생 관점의 성공 경험, `수업 설계 → 학생 활동 → 평가 근거`의 전체 흐름 이해 |
| 2차시 | 1차시에서 체험한 경량앱 사본을 받아 개인 API를 연결하고 목표·성취기준·평가기준·수업자료·학생 참여코드를 입력한다. 사본에서 최초 웹앱 배포를 한 뒤 교사 설정 화면의 전용 주소를 열어 `99-999`로 점검한다. | 내 수업 질문챗봇 | 각자의 개인 수업 챗봇 설정·배포주소·미리보기 완료 |
| 3차시 | 학생용 링크와 숫자 6자리 참여코드를 제한된 실습 집단에 배포하고 결과 시트에서 자동 판단을 검수·수정한다 | 경량앱 + 교사 Sheet | 자동 판단 → 교사 검수 → 최종 평가 경험 |
| 후속과제 | 새 수업을 복제해 3명 이상에게 적용하고 다음 수업 개선안을 쓴다 | 교사 소유 사본 | 반복 가능한 개인 운영 완성 |

### 5.1 1차시 실습 시나리오 — 60분 기준

| 구간 | 진행 | 연수생이 확인할 핵심 |
| --- | --- | --- |
| 0~8분 | 백워드 설계와 전체 흐름을 짧게 안내하고 강사가 미리 만든 학생용 챗봇 주소를 제공 | 결과물을 본 뒤 기준을 만드는 것이 아니라 목표·성취기준·평가기준을 먼저 정한다는 전제 확인 |
| 8~28분 | 연수생이 학생 역할로 반-번호를 입력하고 질문·힌트·생각 수정·종료를 체험 | 설명보다 먼저 실제 학생 화면과 자연스러운 대화 흐름 경험 |
| 28~43분 | 강사가 경량 교사앱의 설정 화면을 열어 목표→평가기준→자료→모드가 방금 체험한 챗봇으로 이어지는 구조를 시연 | 학생 화면 뒤에 있는 교사의 백워드 설계와 설정 이해 |
| 43~53분 | 강사가 학생 응답과 자동 관찰값이 자신의 Sheet에 들어온 모습을 시연 | 챗봇 대화가 과정중심평가의 근거로 남는 방식 이해 |
| 53~60분 | 연수생용 경량앱 사본과 `내 수업 설정` 화면을 열어 2차시 제작 과정을 예고 | 다음 차시의 개인 API 연결과 자기 챗봇 제작 출발점 확보 |

1차시에는 연수생 개인 API를 요구하거나 각자의 챗봇을 완성시키지 않는다. 강사가 경량 교사앱으로 미리 만든 완성본에서 학생 역할의 성공 경험을 먼저 만들고, **같은 앱을 교사 관점으로 다시 열어 본 뒤** 2차시부터 개인 설정과 자기 챗봇 제작으로 넘어간다. 이렇게 하면 연수생이 처음부터 설정 부담을 지지 않으면서도 체험한 화면 구조와 학습 흐름이 개인 제작본으로 이어진다.

## 6. 사용자 화면

### 6.1 교사 설정 앱 — Google Sheet 전용 비공개 화면

학생에게 공개되는 웹주소에 관리자 화면을 넣지 않는다. 교사는 자신이 소유한 Google Sheet의 `내 수업 질문챗봇` 메뉴에서 설정 앱을 연다.

1. **API 연결**: 키 저장, 연결 확인, 키 삭제
2. **평가 설계**: 수업 목표, 성취기준, 평가기준, 수준별 기준, 수집할 평가 근거
3. **수업자료**: 제목, 본문, 자료 링크, 시작 질문, 평가/자료 탐색 모드, 숫자 6자리 학생 참여코드
4. **배포 전 점검**: 누락 항목, API·기존 질문중심 챗봇 실제 연결, 학생용 배포주소, 현재 수업의 교사 전용 미리보기 기록 확인

### 6.2 학생 웹앱 — 공개 화면

- 반-번호와 교사가 수업 시간에 안내한 숫자 6자리 참여코드를 입력한다. 예: `3-12`
- 교사가 승인한 수업자료를 읽고 질문한다.
- 학생 화면에는 API 키, 교사 평가기준 원문, 교사용 메모를 보내지 않는다.
- 미리보기는 교사 설정 화면이 발급한 현재 수업 전용·만료형 주소에서만 열리며, 정확히 `99-999`인 기록만 실제 통계에서 제외한다.
- 강사 운영본과 교사 개인 운영본 모두 같은 화면 뼈대와 빠른 활동 버튼을 사용한다.
- 데스크톱에서는 `수업자료 | 대화`를 함께 보고, 모바일에서는 같은 두 영역을 탭으로 전환한다.
- 대화 세션은 브라우저 탭별 임시 기기값과 수업 버전으로 분리하며, `이 기기에서 나가기` 또는 탭 종료로 다음 사용자가 이전 대화를 이어 보지 않게 한다.
- 공통 참여코드는 공개 링크만 아는 외부인의 우발적 진입을 줄이지만 학생 개인 인증은 아니다. 반-번호는 평가 기록을 묶는 익명 표지이며, 같은 번호로 다른 기기에서 접속해도 기존 대화는 보이지 않고 새 세션이 시작된다. 교사는 수업 중 번호 입력과 중복 접속을 확인한 뒤 최종 평가를 확정한다.

### 6.3 교사 결과 Sheet

| 시트 | 목적 |
| --- | --- |
| `시작하기` | 설정 순서와 현재 준비 상태 |
| `수업 자료` | 현재 수업의 백워드 설계와 자료 원본 |
| `학생별 현황` | 접속 세션별 참여·질문 수·마지막 활동, 같은 반-번호의 중복 접속 표시 |
| `질문과 답변` | 학생과 챗봇의 전체 대화 근거 |
| `교사 평가` | 접속 세션별 자동 판단과 근거, 교사 판단, 피드백, 향상 방법, 다음 수업 |

API 키는 어느 시트에도 기록하지 않는다.

## 7. 기능 요구사항

| ID | 요구사항 | 완료 조건 |
| --- | --- | --- |
| FR-01 | 사본 최초 실행 시 필요한 다섯 시트를 만든다 | 같은 메뉴를 다시 실행해도 중복 시트·머리글이 생기지 않음 |
| FR-02 | 개인 API 키를 안전하게 저장·시험·삭제한다 | 키가 HTML 응답·시트·로그에 노출되지 않음 |
| FR-03 | 백워드 설계 필수값을 입력한다 | 목표·성취기준·평가기준·수준별 기준·평가 근거가 없으면 저장 차단 |
| FR-04 | 수업자료·운영 모드·공통 참여코드를 입력한다 | 30자 이상 자료와 시작 질문, 평가/자료 탐색 모드, 숫자 6자리 참여코드 저장 |
| FR-05 | 교사 설정 화면에서 준비 상태를 한눈에 보여 준다 | 설정 준비/배포 준비를 구분하고 누락 항목을 한국어로 안내 |
| FR-06 | 학생 화면에는 필요한 수업정보만 제공한다 | API 키·루브릭·교사 메모 제외 |
| FR-07 | 기존 질문중심 웹 챗봇의 공통 엔진에서 대화 계획을 받는다 | 같은 `newscomment-ai` 앱의 응답이 `questioning-dialogue-v2` 공통 코어임을 확인하고, 엔진 실패 시 AI 호출 없이 재시도 안내와 실패 상태 저장 |
| FR-08 | 교사 Apps Script가 개인 API로 모델을 호출한다 | API 키가 중앙 엔진으로 전달되지 않음 |
| FR-09 | 대화와 자동 판단을 교사 Sheet에 기록한다 | 첫 실제 질문 때 시작 질문까지 한 번에 저장하고, 이후 학생 1턴당 학생·챗봇 2행을 저장하며 미리보기와 접속 세션을 구분 |
| FR-10 | 교사가 자동 판단을 수정하고 최종 확정한다 | 자동 판단과 교사 판단을 별도 열에 보존 |
| FR-11 | 학생 링크·QR 안내와 배포 점검표를 제공한다 | 필수 준비가 끝나기 전에는 배포 완료로 표시하지 않고, 브라우저의 공유→QR 기능을 안내함 |
| FR-12 | 강사 운영본과 개인 운영본의 학생 경험을 맞춘다 | 핵심 화면 구조, 입장 방식, 빠른 활동, 종료 흐름을 같은 계약으로 구현하고 회귀 시나리오의 정책 결과가 일치 |
| FR-13 | 개인 API 출력은 중앙 엔진이 최종 검증한다 | 허용된 짧은 연결 문구와 수업자료에 실제 포함된 근거 원문만 사용하고, 실패하면 중앙 기본 답변으로 대체 |
| FR-14 | 비용 폭주와 중복 호출을 막는다 | 개인 모델 분·일 한도, 중앙 엔진 분·일·세션 한도, 요청·세션 잠금, 유료 응답 복구 기록을 적용하고 안전 상태를 확인하지 못하면 모델을 다시 호출하지 않음 |
| FR-15 | 학생과 교사 미리보기의 입장을 구분한다 | 일반 입장은 6자리 참여코드를 서버에서 검증하고, `99-999` 미리보기는 현재 수업 전용·만료형 교사 주소에서만 허용함 |

## 8. 기존 질문중심 챗봇과 경량앱의 경계

별도의 중앙 서비스를 두지 않는다. 기존 질문중심 챗봇이 운영되는 하나의 `newscomment-ai` 앱 안에 경량앱용 두 경로를 추가하며, 교사 API 키를 그 앱으로 보내지 않기 위해 호출을 둘로 나눈다.

1. 개인 설정 앱이 익명화한 대화 상태와 웹 챗봇 형식으로 정규화한 수업 설정을 기존 앱의 `https://newscomment-ai.vercel.app/api/lite-engine/plan`에 보낸다.
2. 이 경로는 실제 웹 챗봇도 사용하는 공통 코어에서 자료 해석, 다음 대화 국면, 안전 판정과 관리 질문을 계산한다.
3. 기존 앱은 정책 버전과 모델 호출 계획만 돌려주며, 교사 API 키는 받지 않는다.
4. 개인 설정 앱이 교사 API 키로 허용된 모델을 최대 한 번 호출해 허용된 짧은 연결 문구와 근거 원문을 고르고, 같은 앱의 `/api/lite-engine/finalize`가 계획 식별값·정책 버전·근거 원문의 실제 포함 여부를 확인한 뒤 공통 기본 답변과 함께 최종 문장을 조립한다.
5. 개인 설정 앱이 응답과 평가 근거를 교사 Sheet에 저장한다.

```text
강사 운영본 또는 교사 개인본
  → 기존 newscomment-ai /api/lite-engine/plan
  → questioning-dialogue-v2가 대화·안전·국면·평가 관찰 계획 반환
  → 교사 Apps Script가 교사 개인 API를 최대 1회 호출
  → 기존 newscomment-ai /api/lite-engine/finalize가 근거·정책 최종 확인
  → 교사 소유 Google Sheet에 대화·자동 판단·교사 최종 평가 저장
```

두 경량앱 경로는 기본적으로 무상태로 동작하고 학생 대화 원문을 장기 보관하지 않는다. 기존 질문중심 챗봇 앱이 멈추면 경량앱은 임의의 답을 만들지 않고 “잠시 뒤 다시 시도해 주세요”라고 안내한다. 두 경로는 배포 패키지와 서버가 공유하는 연결키 및 사본별 배포 식별자를 확인하며, 실제 연결키는 저장소에 커밋하지 않는다. 이 공유키는 오남용 억제와 배포본 식별을 위한 자격증명이며 개별 교사를 인증하는 강한 비밀은 아니다. 유출 시 서버 키를 회전하고 새 키를 주입한 교사 사본으로 교체한다.

## 9. 개인정보·보안 원칙

- 반-번호 외 개인정보 입력을 안내하지 않는다. 실수로 입력된 `이름·성명` 표지, 이메일, 대표적인 전화번호·주민번호형 숫자·도로명 상세 주소와 계좌번호 표지는 모델 호출과 Sheet 저장 전에 가린다.
- 가림 규칙은 모든 자유 문장의 개인정보를 완벽히 알아내는 본인 확인 도구가 아니므로, 교사는 학생에게 개인정보를 질문하거나 붙여 넣게 하지 않는다.
- 관리자 설정 화면은 공개 학생 웹앱 경로로 제공하지 않는다.
- API 키는 Script Properties에만 저장하고 읽기 API로 되돌려 주지 않는다.
- 학생용 웹앱은 교사 계정으로 실행해 학생에게 Sheet 권한 승인을 요구하지 않는다.
- 기존 질문중심 챗봇의 경량앱 경로 요청에는 교사 API 키, 교사 이메일, Google Sheet ID를 포함하지 않는다.
- 교사 보드에는 학생 질문 전문을 공개하지 않고, 전문은 교사 Sheet에서만 확인한다.
- 공통 참여코드와 반-번호는 학생 개인 인증 정보가 아니다. 제한된 교실에서 링크의 우발적 외부 사용을 줄이는 장치이며, 원격·무감독 운영에서 학생 신원을 보장해야 한다면 명부별 일회용 PIN 또는 학교 인증을 후속 보안 기능으로 추가한다.
- 같은 반-번호가 여러 기기에서 쓰이면 질문·점수·교사 최종 판단을 세션별로 분리하고 대시보드에 중복 접속 경고를 표시한다. 서로 다른 접속의 최고점을 한 평가로 자동 병합하지 않는다.

## 10. 오류·비용 정책

- 평가모드는 승인된 수업자료 중심으로 답하고 자료 밖 모델 호출을 줄인다.
- 자료 탐색모드는 평가 판정 부담 없이 본문 안의 질문을 넓히되, 검증할 수 없는 본문 밖 사실에는 개인 API를 사용하지 않는다.
- 한 학생 턴에 모델 호출은 최대 한 번이다.
- 교사 개인 배포본 전체에서 개인 API 모델 호출을 분당 60회·로컬 날짜 기준 하루 300회로 제한하며, 한도 확인 잠금에 실패해도 모델을 호출하지 않는다.
- 공통 엔진 요청은 교사 개인 배포본 전체에서 분당 90회·로컬 날짜 기준 하루 900회, 한 학생 번호·수업에서 기기 변경을 포함해 20회로 제한한다. 기존 운영 앱도 배포본·학생 세션별 단기 한도를 별도로 적용한다.
- 운영 앱의 현재 메모리 단기 한도는 서버리스 인스턴스 전체를 합산하는 과금 보장 장치가 아니다. 교사 Apps Script의 요청·개인 API 한도를 1차 비용 경계로 두고, 불특정 다수에게 배포하기 전에는 호스팅 계층 제한 또는 공유 저장소 기반 제한을 추가한다.
- 기존 챗봇 연결, 개인 API, Sheet 기록 중 어디서 실패했는지 교사 결과에 구분한다.
- 같은 요청의 중복 제출을 요청 ID와 요청·세션·학생-수업 잠금으로 막는다. 유료 응답은 Sheet 기록 전에 `provider_started → candidate_ready → result_ready` 복구 기록에 저장하여 통신 또는 기록 실패 뒤에도 같은 요청 ID로 결과만 복구하고 모델을 다시 호출하지 않는다. 호출 시작 뒤 후보 저장 전 실행이 끊겨 과금 여부를 판정할 수 없으면 자동 재호출을 중단하고 교사 확인으로 전환한다.
- 브라우저에는 현재 수업과 독립된 활성 전송 포인터를 세션 동안 보관한다. 새로고침 뒤에는 현재 수업 입장 검사보다 기존 전송 복구를 먼저 시도하므로, 교사가 수업을 닫거나 자료를 개정한 직후에도 이미 처리된 Q&A·학생 현황·평가 초안을 원래 수업 ID와 개정 번호에 마무리한다. 기존 기록이 전혀 없는 새 요청은 닫힌·개정 전 수업을 우회할 수 없다.
- 요청 시작 시점의 API 검증값과 중앙 엔진 endpoint·정책 세대를 fingerprint로 고정한다. 중간에 세대가 바뀌면 과거 유료 후보를 새 엔진으로 finalize하지 않고, 당시 중앙 계획에 함께 저장된 안전한 기본 답변을 기록하며 새 세대의 `99-999` 미리보기를 통과시키지 않는다. 같은 주소에서 중앙 정책 버전이 바뀐 응답을 받으면 이전 검증 표지가 여전히 현재 값일 때만 원자적으로 해제하여 교사 화면을 `엔진 재연결 확인 → 99-999 재미리보기`로 돌린다.
- 학생 입장만으로는 Sheet에 행을 만들지 않는다. 시작 질문은 첫 실제 발화를 기록하는 같은 작업에서 한 번만 저장한다.
- API 제한이나 동시 접속 오류는 학생에게 쉬운 재시도 문구로 표시한다.

## 11. 구현 순서와 완료 기준

### 0단계 — PRD와 별도 프로젝트 구조

- `gas-lite/`를 기존 v37의 `gas/`와 분리한다.
- 기존 공개 배포와 시트 계약을 변경하지 않는다.

### 1단계 — 교사용 설정 마법사

- 다섯 시트 자동 준비
- API 저장·연결 확인·삭제
- 평가 설계 및 수업자료 저장
- 설정 준비도 표시
- 학생 미리보기 껍데기

완료 기준: 잘못된 필수값이 저장되지 않고, 저장 후 다시 열었을 때 값이 유지되며, 학생용 데이터에 API 키와 평가기준이 포함되지 않는다.

### 2단계 — 학생 대화와 개인 Sheet 기록

- 학생 코드·공통 6자리 참여코드 검증과 세션 생성
- 브라우저 탭별 세션 분리와 명시적 나가기
- 입장 단계 무기록, 첫 실제 턴에서 시작 질문·학생 발화·챗봇 응답 원자적 저장
- 학생/챗봇 턴 저장
- 현재 수업 전용·만료형 교사 주소에서 정확히 `99-999`인 미리보기만 통계 제외
- 중복 제출 방지

### 3단계 — 기존 질문중심 챗봇의 공통 엔진 연결

- `/api/lite-engine/plan`·`/api/lite-engine/finalize` 요청·응답 계약 구현
- 실제 웹 챗봇과 같은 설정 형식·로컬 대화 코어·2국면 정책을 직접 공유
- 별도 서비스가 아닌 기존 `newscomment-ai` 배포에 두 경로를 포함하고, 서버 환경변수와 교사용 배포 패키지의 연결키를 일치시킴
- 평가/자료 탐색 모드는 공통 코어 결과 위에서 기록 목적을 달리하며, 개인 API는 검증 가능한 본문 근거 선택에만 사용
- 장애 시 안전한 중단

### 4단계 — 개인 API 모델 호출

- 기존 챗봇의 계획을 받아 교사 Apps Script가 모델을 한 번 호출
- 비용·오류 상태 기록
- 49회기 정책 회귀 평가

### 5단계 — 교사 검수와 평가 포워드

- 자동 판단·근거 초안
- 평가모드에서 기준별 최고 관찰값과 고유 근거를 누적하고, 관리 질문에 대한 유효 응답은 단어 분류와 무관하게 보존
- 근거 문구가 같아도 새 학생 요청이면 새 관찰로 기록해 이미 확정한 평가는 `재검수 필요`로 되돌리고, 같은 요청의 저장 복구만 중복 제거
- 대시보드를 연 뒤 학생 근거가 추가되거나 다른 교사 검수가 저장되면 오래된 카드의 저장을 거절하고 새로고침 후 다시 검수
- 교사 수정·최종 확정
- 학생 향상 방법과 다음 수업 제안
- 후속: 1회성 세특 및 누적 성장 자료 내보내기

### 6단계 — 배포 안내

- 학생 링크·6자리 참여코드와 브라우저 공유→QR 안내
- API·기존 질문중심 챗봇·웹앱 배포 사전 점검과 현재 수업 전용 `99-999` 미리보기
- 사본별 최초 1회 `배포 → 새 배포 → 웹 앱` 안내
- 수업 종료·복제

### 7단계 — 연수 실증

- 교사 초보자 3명 설정 관찰
- 학생 역할 25명 동시 제출과 30명 2개 모둠 분산 제출
- 설정 완료 시간, 실패 지점, API 비용, 응답시간 기록

## 12. 출시 판정

코드 수준 MVP는 자동 회귀검사와 빌드가 모두 통과하면 완료로 표시한다. 다음 현장 검증 조건까지 만족해야 실제 연수생 배포본으로 표시한다.

- 초보 교사가 설명을 보며 20분 안에 설정·미리보기를 완료
- API 키가 Sheet·학생 HTML·기존 질문중심 챗봇 요청에서 발견되지 않음
- 25명 동시 제출과 30명 2개 모둠 분산 제출에서 기록 누락·중복 모델 호출 0건
- 평가모드·자료 탐색모드 회귀 평가 통과
- 같은 입력에 대해 강사 운영본과 개인 운영본의 안전·종료·국면·관리 질문 결과가 일치
- 학생 화면의 지문/대화 구조와 핵심 조작이 강사 운영본과 개인 운영본에서 동등하게 동작함
- 자동 판단을 교사가 검수하지 않은 상태가 “최종 평가”로 표시되지 않음
- 최종 확정 뒤 새 학생 요청이 들어오면 근거 문구·점수가 같아도 `재검수 필요`가 되고, 오래 열린 화면으로 이를 덮어쓸 수 없음
- 현재 수업 안에서 학생 결과, 교사 피드백, 향상 방법이 같은 평가 행에 함께 저장됨
- 수업 중 번호 오입력·중복 접속이 다른 학생의 기존 대화를 노출하거나 해당 번호의 입장을 영구 차단하지 않음
- 잘못된 참여코드와 일반 학생 주소의 `99-999` 입력으로 수업에 들어갈 수 없음
- Google Sheet 사본에서 최초 웹앱 배포를 마치고 학생 주소가 실제로 열림

`1회성 세특·종합 세특·여러 수업의 누적 성장 보드`는 이 MVP의 출시 차단 조건이 아니라 2차 후속 범위다. MVP에서는 한 활성 수업 안에서 자동 판단→교사 검수→최종 평가와 향상 방법 기록까지 완결한다.
