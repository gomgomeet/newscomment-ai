# simbot 필수 두 문항 평가

교사는 지문과 성취기준 또는 수업 목표를 입력하고, AI가 만든 두 문항을 수정·확정한다. 확정한 문항으로 문항별 기준표를 생성한 뒤, 문항·기준표·보충 질문을 검토하고 승인하여 저장한다. 기존 기준별 질문계획과 대화 진행 기록은 유지한다.

## 교사 사용 순서

1. 교사 설정에서 평가모드를 켜고 평가 방식을 **필수 2문항 평가**로 선택한다.
2. 본문, 성취기준 또는 수업 목표, 평가 수준을 입력한다. 3단계, 4단계(매우잘함·잘함·보통·노력요함), 5단계(A~E)를 지원한다.
3. 두 문항을 생성하고 교사가 수정한 뒤 확정한다.
4. 문항별 분석 기준표를 생성한다. 핵심 답변 요소, 지문의 실제 근거, 수준별 기준과 예상 답변 유형을 확인·수정한다.
5. 두 문항과 기준표, 근거 확인용 보충 질문을 검토·승인하고 수업을 저장한다.
6. 교사 전용 99-999 미리보기로 대화를 확인한 뒤 학생에게 수업 주소와 참여코드를 안내한다.
7. 교사 대시보드에서 문항별 답변·실제 인용·AI 판단 근거를 보고 최종 판단을 저장한다.

지문·목표·성취기준·질문·수준 체계가 바뀌면 기준표를 다시 생성한다. 기준 문구만 수정하면 표를 유지하고 검토 승인을 다시 받는다. 기존 수업은 기존 평가 방식으로 남아 있으며, 교사가 새 방식으로 바꾸고 저장해야 적용된다.

## 학생 대화와 분석

- 학생은 별도 답안 폼 없이 승인된 두 문항에 차례로 답한다. 필요하면 각 문항의 근거를 확인하는 보충 질문이 이어진다.
- 서버가 저장된 대화의 문항별 답변 사건과 원문을 연결한다. 일반 질문, 도움 요청, 건너뛰기를 평가 답변으로 혼동하지 않는다.
- 두 문항 진행이 끝나면 별도 호출로 AI 분석을 수행한다. 학생 화면에는 기록 상태만 보이고, 기준표·예상 답변·AI 수준 판정은 교사만 본다.
- 답변이 없는 문항은 **판단 보류**로 처리한다. AI는 문항별 초안만 작성하며 평균·총점·최종 성적은 자동 확정하지 않는다.
- 학생이 완료 직후 브라우저를 닫아 분석 호출이 빠져도 교사 대시보드에서 저장된 대화를 확인하고 **답변 분석**을 실행할 수 있다.

## 저장과 복구

- `수업 자료`: `requiredAssessmentMode`, `requiredAssessmentJson`을 추가하고, 기존 `assessmentPlanJson`에 확정된 두 문항을 연결한다.
- `질문과 답변`: 기존 `assessmentProgressJson`과 답변 사건을 보존한다.
- `필수 평가 응답`: 세션·수업 개정·자료 해시·문항 집합 해시·답변·분석 결과와 비용 정보를 기록한다.
- `교사 평가`: `questionId`로 두 문항을 구분하고 교사 판단을 보존한다.
- 유료 요청 전에 답변을 기록하여 중복 요청을 막는다. 결과 저장 실패 시 보관된 결과를 복구한다. 호출 결과가 불확실하거나 분석 검증에 실패하면 자동 재과금 없이 교사 검토 대기로 둔다.
- 분석 인용이 실제 학생 발화 안에 있는지 검사한다. 두 발화를 이어 붙여 하나의 인용으로 제시한 결과는 거부한다.
- `99-999` 미리보기 기록은 실제 학생 평가에서 제외한다.

## 검증 명령

```sh
npm run test:required-assessment
npm run test:gas-lite
node evals/gas/run.mjs
```

자동 검사는 문항 생성·교사 수정 보존·승인·중앙 질문계획 호환, 기존 시트 열 보존, 대화 답변 연결, 중복 호출 및 저장 복구, 실제 인용 검사, 미응답 처리, 교사 최종 판단 보존을 포함한다. AI가 만든 질문과 수준 예시의 교육적 적절성은 교사가 검토한다.
